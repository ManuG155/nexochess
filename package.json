{
    "workspaces": [
        "./shared",
        "./server",
        "./client"
    ],
    "scripts": {
        "start": "node server/dist/index.js",
        "build": "npm run build --workspaces --if-present",
        "build:cloudflare": "npm run build -w shared && npm run build -w client && node scripts/build-cloudflare.mjs",
        "bbuild": "npm run build -w shared && npm run build -w server",
        "dev": "npm run build && npm start",
        "lint": "eslint .",
        "check:puzzles": "node scripts/check-puzzle-backend.mjs",
        "check:translations": "node scripts/check-translations.mjs",
        "check:repository": "node scripts/check-repository.mjs",
        "check": "npm run check:translations && npm run check:repository && npm run check:puzzles && npm run check -w client && npm run check -w server",
        "verify": "npm run check && npm run build",
        "import:puzzles": "node scripts/import-lichess-puzzles.mjs",
        "export:puzzles:d1": "node scripts/export-puzzles-d1-sql.mjs"
    },
    "devDependencies": {
        "@stylistic/eslint-plugin-ts": "^2.1.0",
        "eslint": "^8.57.1",
        "typescript": "^5.4.5",
        "typescript-eslint": "^7.12.0",
        "webpack-bundle-analyzer": "^4.10.2"
    }
}
