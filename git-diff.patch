diff --git a/client/public/locales/es/analysis.json b/client/public/locales/es/analysis.json
index 92d2b96..9cffc29 100644
--- a/client/public/locales/es/analysis.json
+++ b/client/public/locales/es/analysis.json
@@ -70,20 +70,36 @@
     "accuraciesCard": {
         "title": "Precisiones"
     },
-    "classifiedMoveCard": {
+        "classifiedMoveCard": {
         "unknownError": "No se ha podido cargar la clasificaci├│n.",
         "insufficientLines": "Aseg├║rese de que la jugada anterior sea evaluada completamente para una clasificaci├│n.",
         "classifications": {
-            "brilliant": " es brillante",
+            "brilliant": "es brillante",
+            "critical": "es una jugada genial",
             "best": "es la mejor jugada",
             "excellent": "es excelente",
-            "okay": "es una jugada correcta",
-            "inaccuracy": " es una imprecisi├│n",
-            "mistake": " es un error",
-            "blunder": " es un error grave",
-            "forced": " es forzado",
+            "okay": "es una buena jugada",
+            "inaccuracy": "es una imprecisi├│n",
+            "mistake": "es un error",
+            "miss": "es una omisi├│n",
+            "blunder": "es un error grave",
+            "forced": "es forzada",
             "theory": "es te├│rica",
             "risky": "es una idea arriesgada"
         }
+    },
+    "classifications": {
+        "brilliant": "Brillante",
+        "critical": "Genial",
+        "best": "Mejor",
+        "excellent": "Excelente",
+        "okay": "Bueno",
+        "inaccuracy": "Imprecisi├│n",
+        "mistake": "Error",
+        "miss": "Omisi├│n",
+        "blunder": "Error grave",
+        "forced": "Forzada",
+        "theory": "Libro",
+        "risky": "Arriesgada"
     }
 }
\ No newline at end of file
diff --git a/client/src/apps/features/analysis/components/AnalysisPanel/AnalysisPanel.module.css b/client/src/apps/features/analysis/components/AnalysisPanel/AnalysisPanel.module.css
index 7a1955e..7f99837 100644
--- a/client/src/apps/features/analysis/components/AnalysisPanel/AnalysisPanel.module.css
+++ b/client/src/apps/features/analysis/components/AnalysisPanel/AnalysisPanel.module.css
@@ -1,70 +1,193 @@
 .wrapper {
     display: flex;
     flex-direction: column;
+
     box-sizing: border-box;
 
     border-radius: 10px;
-    background-color: var(--ui-shade-3);
 
-    overflow-x: hidden;
+    background-color:
+        transparent;
+
+    overflow-x:
+        hidden;
 }
 
+
+/*
+ * CONTENIDO PRINCIPAL
+ */
 .components {
     display: flex;
     flex-grow: 1;
     flex-direction: column;
+
     gap: 10px;
+
+    min-height: 0;
+
     padding: 10px;
 
     overflow-y: auto;
-    overscroll-behavior: contain;
+
+    overscroll-behavior:
+        contain;
 }
 
-.title {
-    text-align: center;
-    padding: 5px 10px;
 
-    border-radius: 10px;
-    background-color: var(--ui-blue);
-    color: white;
-    font-family: "Nunito";
-    font-weight: 600;
-    font-size: 2.5rem;
+/*
+ * CABECERA TEMPORAL
+ * DEL MODO REVIEW.
+ */
+.reviewHeader {
+    display: flex;
+
+    justify-content:
+        flex-start;
 
-    overflow-wrap: break-word;
+    align-items:
+        center;
+
+    width: 100%;
 }
 
-@media (max-width: 900px) {
-    .title {
-        display: none;
-    }
+
+/*
+ * Flecha temporal para
+ * volver al resumen.
+ *
+ * En un bloque posterior
+ * la refinaremos visualmente.
+ */
+.reviewBackButton {
+    display: flex;
+
+    justify-content:
+        center;
+
+    align-items:
+        center;
+
+    width: 34px;
+    height: 34px;
+
+    padding: 0;
+
+    border: 0;
+
+    border-radius:
+        7px;
+
+    background-color:
+        rgba(
+            255,
+            255,
+            255,
+            0.08
+        );
+
+    color:
+        #FFFFFF;
+
+    font-size:
+        1.2rem;
+
+    cursor:
+        pointer;
+
+    transition:
+        background-color
+        150ms
+        ease;
+}
+
+
+.reviewBackButton:hover {
+    background-color:
+        rgba(
+            255,
+            255,
+            255,
+            0.14
+        );
 }
 
+
+/*
+ * CONTENEDOR DE:
+ *
+ * |< ÔåÉ ÔûÂ ÔåÆ >|
+ */
 .traverserContainer {
     display: flex;
-    justify-content: center;
+
+    justify-content:
+        center;
+
     width: 100%;
 }
 
-@media (max-width: 900px) {
-    .traverserContainer {
-        box-sizing: border-box;
-        padding: 0 10px;
-        position: fixed;
-        bottom: 10px;
-        left: 0;
-    }
-}
 
+/*
+ * Toolbar de navegaci├│n.
+ *
+ * M├ís adelante esto
+ * desaparecer├í como bloque
+ * y ser├ín cinco botones
+ * flotantes individuales.
+ */
 .traverser {
-    width: min(100%, 420px);
-    background-color: #1b1c1e;
+    width:
+        min(
+            100%,
+            420px
+        );
+
+    background-color:
+        #1B1C1E;
 }
 
-@media (max-width: 900px) {
+
+/*
+ * PANTALLAS PEQUE├æAS
+ */
+@media (
+    max-width: 900px
+) {
+
+    .traverserContainer {
+        position:
+            fixed;
+
+        bottom:
+            10px;
+
+        left:
+            0;
+
+        box-sizing:
+            border-box;
+
+        padding:
+            0 10px;
+    }
+
+
     .traverser {
-        border-radius: 10px;
-        background-color: #27292f;
-        box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
+        border-radius:
+            10px;
+
+        background-color:
+            #27292F;
+
+        box-shadow:
+            0 0 10px
+            rgba(
+                0,
+                0,
+                0,
+                0.5
+            );
     }
+
 }
\ No newline at end of file
diff --git a/client/src/apps/features/analysis/components/AnalysisPanel/index.tsx b/client/src/apps/features/analysis/components/AnalysisPanel/index.tsx
index 59e3228..fcea453 100644
--- a/client/src/apps/features/analysis/components/AnalysisPanel/index.tsx
+++ b/client/src/apps/features/analysis/components/AnalysisPanel/index.tsx
@@ -1,76 +1,455 @@
-import React, { lazy } from "react";
+import React, {
+    useEffect,
+    useState
+} from "react";
 
-import AnalysisTab from "@analysis/constants/AnalysisTab";
-import useSettingsStore from "@/stores/SettingsStore";
-import useAnalysisGameStore from "@analysis/stores/AnalysisGameStore";
-import useAnalysisBoardStore from "@analysis/stores/AnalysisBoardStore";
-import useAnalysisTabStore from "@analysis/stores/AnalysisTabStore";
-import ClassifiedMoveCard from "@analysis/components/report/ClassifiedMoveCard";
-import StateTreeTraverser from "@/components/chess/StateTreeTraverser";
+import useSettingsStore from
+    "@/stores/SettingsStore";
 
-import TabBar from "./TabBar";
-import AnalysisProgress from "./AnalysisProgress";
-import RealtimeEngineArea from "./RealtimeEngineArea";
+import useAnalysisGameStore from
+    "@analysis/stores/AnalysisGameStore";
 
-import GameSelection from "./GameSelection";
-import GameReport from "./GameReport";
-import GameAnalysis from "./GameAnalysis";
+import useAnalysisBoardStore from
+    "@analysis/stores/AnalysisBoardStore";
 
-import AnalysisPanelProps from "./AnalysisPanelProps";
-import * as styles from "./AnalysisPanel.module.css";
+import ClassifiedMoveCard from
+    "@analysis/components/report/ClassifiedMoveCard";
+
+import StateTreeTraverser from
+    "@/components/chess/StateTreeTraverser";
+
+import AnalysisProgress from
+    "./AnalysisProgress";
+
+import RealtimeEngineArea from
+    "./RealtimeEngineArea";
+
+import GameSelection from
+    "./GameSelection";
+
+import GameAnalysis from
+    "./GameAnalysis";
+
+import GameSummaryPanel from
+    "./GameSummaryPanel";
+
+import AnalysisPanelProps from
+    "./AnalysisPanelProps";
+
+import * as styles from
+    "./AnalysisPanel.module.css";
+
+
+/*
+ * Los dos ├║nicos modos que
+ * tendr├í el panel despu├®s
+ * de analizar una partida.
+ *
+ * summary:
+ * resumen de la partida.
+ *
+ * review:
+ * revisi├│n movimiento a movimiento.
+ */
+type SidePanelMode =
+    | "summary"
+    | "review";
 
-const OptionsToolbar = lazy(() => import("@analysis/components/OptionsToolbar"));
 
 function AnalysisPanel({
     className,
     style
 }: AnalysisPanelProps) {
 
-    const settings = useSettingsStore(state => state.settings.analysis);
+    /*
+     * Ajustes del an├ílisis.
+     */
+    const settings =
+        useSettingsStore(
+            state =>
+                state.settings.analysis
+        );
 
-    const gameAnalysisOpen = useAnalysisGameStore(
-        state => state.gameAnalysisOpen
-    );
 
-    const currentNode = useAnalysisBoardStore(
-        state => state.currentStateTreeNode
+    /*
+     * Partida actualmente analizada.
+     */
+    const analysisGame =
+        useAnalysisGameStore(
+            state =>
+                state.analysisGame
+        );
+
+
+    /*
+     * Indica si ya hemos abierto
+     * el an├ílisis de una partida.
+     */
+    const gameAnalysisOpen =
+        useAnalysisGameStore(
+            state =>
+                state.gameAnalysisOpen
+        );
+
+
+    /*
+     * Posici├│n actualmente
+     * mostrada en el tablero.
+     */
+    const currentNode =
+        useAnalysisBoardStore(
+            state =>
+                state.currentStateTreeNode
+        );
+
+
+    /*
+     * Permite cambiar la posici├│n
+     * mostrada por el tablero.
+     */
+    const setCurrentStateTreeNode =
+        useAnalysisBoardStore(
+            state =>
+                state.setCurrentStateTreeNode
+        );
+
+
+    /*
+     * Control del autoplay.
+     */
+    const setAutoplayEnabled =
+        useAnalysisBoardStore(
+            state =>
+                state.setAutoplayEnabled
+        );
+
+
+    /*
+     * Nuestro nuevo modo.
+     *
+     * Al terminar un an├ílisis
+     * empezaremos SIEMPRE
+     * en el resumen.
+     */
+    const [
+        sidePanelMode,
+        setSidePanelMode
+    ] = useState<SidePanelMode>(
+        "summary"
     );
 
-    const { activeTab } = useAnalysisTabStore();
-    
-    return <div
-        className={`${styles.wrapper} ${className}`}
-        style={style}
-    >
-        <div className={styles.title}>
 
-            {(gameAnalysisOpen && settings.engine.enabled)
-                && <RealtimeEngineArea/>
+    /*
+     * Si cerramos el an├ílisis
+     * y volvemos al selector PGN,
+     * restauramos autom├íticamente
+     * el modo resumen.
+     *
+     * As├¡ la siguiente partida
+     * nunca empieza accidentalmente
+     * en modo review.
+     */
+    useEffect(
+        () => {
+
+            if (!gameAnalysisOpen) {
+
+                setSidePanelMode(
+                    "summary"
+                );
             }
 
-            {gameAnalysisOpen
-                && currentNode.state.move
-                && !settings.classifications.hide
-                && (
-                    settings.engine.enabled
-                    || currentNode.state.classification
-                )
-                && <ClassifiedMoveCard/>
+        },
+
+        [
+            gameAnalysisOpen
+        ]
+    );
+
+
+    /*
+     * EMPEZAR REVISI├ôN.
+     *
+     * Cuando pulsamos el bot├│n verde:
+     *
+     * 1. detenemos autoplay;
+     * 2. llevamos el tablero
+     *    a la posici├│n inicial;
+     * 3. cambiamos a modo review.
+     */
+    function startReview() {
+
+        setAutoplayEnabled(
+            false
+        );
+
+
+        setCurrentStateTreeNode(
+            analysisGame.stateTree
+        );
+
+
+        setSidePanelMode(
+            "review"
+        );
+    }
+
+
+    /*
+     * VOLVER AL RESUMEN
+     *
+     * M├ís adelante sustituiremos
+     * este bot├│n temporal por
+     * la flecha peque├▒a y limpia
+     * que quieres arriba a la izquierda.
+     */
+    function backToSummary() {
+
+        setAutoplayEnabled(
+            false
+        );
+
+
+        setSidePanelMode(
+            "summary"
+        );
+    }
+
+
+    return (
+        <div
+            className={
+                `${styles.wrapper} ${className}`
             }
 
-            {gameAnalysisOpen
-                ? (activeTab == AnalysisTab.REPORT
-                    ? <GameReport/>
-                    : <GameAnalysis/>
-                )
-                : <GameSelection/>
+            style={
+                style
             }
-        </div>
+        >
+
+            <div
+                className={
+                    styles.components
+                }
+            >
+
+                {/*
+                 * PROGRESO DEL AN├üLISIS.
+                 *
+                 * De momento lo conservamos.
+                 *
+                 * En un bloque posterior
+                 * vamos a:
+                 *
+                 * - ocultar "Solving CAPTCHA";
+                 * - mostrar progreso limpio;
+                 * - sincronizar tablero
+                 *   con movimiento analizado.
+                 */}
+                <AnalysisProgress />
+
+
+                {/*
+                 * =================================================
+                 * 1. NO HAY PARTIDA ANALIZADA
+                 * =================================================
+                 *
+                 * Mostramos selector PGN.
+                 */}
+                {!gameAnalysisOpen && (
+
+                    <GameSelection />
+
+                )}
+
+
+                {/*
+                 * =================================================
+                 * 2. RESUMEN
+                 * =================================================
+                 *
+                 * Ya NO existe:
+                 *
+                 * Report | Analysis
+                 *
+                 * El flujo es ahora:
+                 *
+                 * Resumen
+                 * Ôåô
+                 * Empezar revisi├│n
+                 * Ôåô
+                 * Review
+                 */}
+                {(
+                    gameAnalysisOpen
+
+                    &&
+
+                    sidePanelMode
+                    == "summary"
+                ) && (
+
+                    <GameSummaryPanel
+                        onStartReview={
+                            startReview
+                        }
+                    />
+
+                )}
+
+
+                {/*
+                 * =================================================
+                 * 3. REVISI├ôN
+                 * =================================================
+                 */}
+                {(
+                    gameAnalysisOpen
+
+                    &&
+
+                    sidePanelMode
+                    == "review"
+                ) && (
+                    <>
+
+                        {/*
+                         * Cabecera temporal
+                         * de la revisi├│n.
+                         *
+                         * M├ís adelante
+                         * la convertiremos
+                         * en nuestra cabecera
+                         * compacta B + C.
+                         */}
+                        <div
+                            className={
+                                styles.reviewHeader
+                            }
+                        >
+
+                            <button
+                                type="button"
+
+                                className={
+                                    styles.reviewBackButton
+                                }
+
+                                onClick={
+                                    backToSummary
+                                }
+
+                                aria-label={
+                                    "Volver al resumen"
+                                }
+                            >
+                                ÔåÉ
+                            </button>
+
+                        </div>
+
+
+
+                        {/*
+                         * Motor en tiempo real.
+                         */}
+                        {settings.engine.enabled && (
+
+                            <RealtimeEngineArea />
+
+                        )}
+
+
+                        {/*
+                         * Clasificaci├│n
+                         * de la jugada actual.
+                         */}
+                        {(
+                            currentNode.state.move
+
+                            &&
+
+                            !settings
+                                .classifications
+                                .hide
+
+                            &&
+
+                            (
+                                settings
+                                    .engine
+                                    .enabled
+
+                                ||
+
+                                currentNode
+                                    .state
+                                    .classification
+                            )
+                        ) && (
+
+                            <ClassifiedMoveCard />
+
+                        )}
+
+
+                        {/*
+                         * Lista/├írbol de movimientos.
+                         *
+                         * Este es el antiguo
+                         * modo "Analysis".
+                         */}
+                        <GameAnalysis />
+
+                    </>
+                )}
+
+            </div>
+
+
+            {/*
+             * BOTONES:
+             *
+             * |<  ÔåÉ  ÔûÂ  ÔåÆ  >|
+             *
+             * Solo aparecen durante
+             * la revisi├│n.
+             *
+             * En el resumen
+             * desaparecen completamente.
+             *
+             * M├ís adelante los convertiremos
+             * en cinco botones individuales,
+             * flotantes y separados.
+             */}
+            {(
+                gameAnalysisOpen
+
+                &&
+
+                sidePanelMode
+                == "review"
+            ) && (
+
+                <div
+                    className={
+                        styles.traverserContainer
+                    }
+                >
+
+                    <StateTreeTraverser
+                        className={
+                            styles.traverser
+                        }
+                    />
+
+                </div>
+
+            )}
 
-        <div className={styles.traverserContainer}>
-            <StateTreeTraverser className={styles.traverser} />
         </div>
-    </div>;
+    );
 }
 
+
 export default AnalysisPanel;
\ No newline at end of file
diff --git a/client/src/apps/features/analysis/components/Board/Board.module.css b/client/src/apps/features/analysis/components/Board/Board.module.css
index 6316569..8d08cc0 100644
--- a/client/src/apps/features/analysis/components/Board/Board.module.css
+++ b/client/src/apps/features/analysis/components/Board/Board.module.css
@@ -29,4 +29,85 @@
 .chessboardWrapper {
     position: relative;
     flex-shrink: 0;
+}
+
+/*
+ * N├ÜMEROS 8-1
+ *
+ * Flotan a la izquierda
+ * del tablero.
+ *
+ * No afectan a su tama├▒o.
+ */
+.rankCoordinates {
+    position: absolute;
+
+    top: 0;
+    left: -22px;
+
+    display: grid;
+
+    grid-template-rows:
+        repeat(8, 1fr);
+
+    width: 18px;
+    height: 100%;
+
+    color:
+        rgba(255, 255, 255, 0.65);
+
+    font-size: 0.75rem;
+
+    font-weight: 600;
+
+    pointer-events: none;
+
+    user-select: none;
+}
+
+
+/*
+ * LETRAS A-H
+ *
+ * Flotan debajo
+ * del tablero.
+ */
+.fileCoordinates {
+    position: absolute;
+
+    bottom: -22px;
+    left: 0;
+
+    display: grid;
+
+    grid-template-columns:
+        repeat(8, 1fr);
+
+    width: 100%;
+    height: 18px;
+
+    color:
+        rgba(255, 255, 255, 0.65);
+
+    font-size: 0.75rem;
+
+    font-weight: 600;
+
+    pointer-events: none;
+
+    user-select: none;
+}
+
+
+/*
+ * Centrado de cada n├║mero/letra.
+ */
+.coordinate {
+    display: flex;
+
+    justify-content: center;
+    align-items: center;
+
+    width: 100%;
+    height: 100%;
 }
\ No newline at end of file
diff --git a/client/src/apps/features/analysis/components/Board/index.tsx b/client/src/apps/features/analysis/components/Board/index.tsx
index 21530db..a3e5fd9 100644
--- a/client/src/apps/features/analysis/components/Board/index.tsx
+++ b/client/src/apps/features/analysis/components/Board/index.tsx
@@ -29,6 +29,17 @@ import * as styles from "./Board.module.css";
 type ClickMove = Pick<Move, "from" | "to">;
 
 
+/*
+ * Barra de evaluaci├│n.
+ *
+ * 26 px de ancho.
+ * 10 px de separaci├│n cuando
+ * las coordenadas est├ín dentro.
+ */
+const EVALUATION_BAR_WIDTH = 26;
+const EVALUATION_BAR_GAP = 10;
+
+
 function getPieceType(piece: Piece) {
     return piece.at(1)?.toLowerCase() as PieceSymbol;
 }
@@ -50,32 +61,90 @@ function Board({
     enableClassifications = true,
     onAddMove
 }: BoardProps) {
+
     const squares = useSquares();
 
+
+    /*
+     * Configuraci├│n visual
+     * de nuestras flechas.
+     */
     const arrowStyle = useSettingsStore(
-        state => state.settings.analysis.arrowStyle
+        state =>
+            state.settings.analysis.arrowStyle
+    );
+
+
+    /*
+     * Posici├│n de las coordenadas:
+     *
+     * "inside"
+     * "outside"
+     */
+    const coordinatePosition =
+        useSettingsStore(
+            state =>
+                state.settings
+                    .themes
+                    .board
+                    .coordinates
+        );
+
+
+    const coordinatesOutside =
+        coordinatePosition == "outside";
+
+
+    /*
+     * Renderizado personalizado
+     * de las casillas.
+     */
+    const squareRenderer = useMemo(
+        () => (
+            createSquareRenderer(
+                node,
+                enableClassifications
+            )
+        ),
+        [
+            node,
+            enableClassifications
+        ]
     );
 
-    const squareRenderer = useMemo(() => (
-        createSquareRenderer(node, enableClassifications)
-    ), [
-        node,
-        enableClassifications
-    ]);
 
+    /*
+     * Estado para promociones.
+     */
     const [
         heldPromotion,
         setHeldPromotion
     ] = useState<ClickMove>();
 
+
+    /*
+     * Flechas dibujadas manualmente
+     * con bot├│n derecho.
+     */
     const [
         manualArrows,
         setManualArrows
     ] = useState<Arrow[]>([]);
 
+
+    /*
+     * Referencia al contenedor principal
+     * del tablero.
+     */
     const boardContainerRef =
-        useRef<HTMLDivElement | null>(null);
+        useRef<HTMLDivElement | null>(
+            null
+        );
 
+
+    /*
+     * Ancho disponible.
+     */
     const {
         fullWidth: boardWidth
     } = useResizeObserver(
@@ -83,53 +152,200 @@ function Board({
         1
     );
 
+
     /*
-     * VOLVEMOS AL C├üLCULO QUE FUNCIONABA.
-     * Ma├▒ana reharemos coordenadas y barra
-     * sin tocar la geometr├¡a interna del tablero.
+     * Si las coordenadas est├ín fuera,
+     * necesitamos algo m├ís de separaci├│n
+     * entre la barra de evaluaci├│n
+     * y el tablero para colocar 8-1.
+     *
+     * Inside:
+     * 10 px.
+     *
+     * Outside:
+     * 28 px.
      */
-    const chessboardWidth =
-        boardWidth - (evaluation ? 40 : 0);
+    const evaluationBarGap =
+        coordinatesOutside
+            ? 28
+            : EVALUATION_BAR_GAP;
 
-    const renderedArrows = useMemo(() => [
-        ...(arrows ?? []).map(arrow => ({
-            ...arrow,
-            colour: arrowStyle.suggestionColour
-        })),
 
-        ...manualArrows.map(([from, to]) => ({
-            from,
-            to,
-            colour: arrowStyle.manualColour
-        }))
-    ], [
-        arrows,
-        manualArrows,
-        arrowStyle.suggestionColour,
-        arrowStyle.manualColour
-    ]);
+    /*
+     * Espacio horizontal reservado
+     * para:
+     *
+     * barra + separaci├│n.
+     */
+    const evaluationReservedWidth =
+        evaluation
+            ? (
+                EVALUATION_BAR_WIDTH
+                + evaluationBarGap
+            )
+            : 0;
+
+
+    /*
+     * Tama├▒o real del Chessboard.
+     *
+     * Math.max protege al componente
+     * durante el primer c├ílculo
+     * del ResizeObserver.
+     *
+     * Nunca enviamos boardWidth={0}.
+     */
+    const chessboardWidth = Math.max(
+        boardWidth
+            - evaluationReservedWidth,
+        1
+    );
+
+
+    /*
+     * Coordenadas verticales.
+     *
+     * Normal:
+     * 8 -> 1
+     *
+     * Flip Board:
+     * 1 -> 8
+     */
+    const rankCoordinates = flipped
+        ? [
+            "1",
+            "2",
+            "3",
+            "4",
+            "5",
+            "6",
+            "7",
+            "8"
+        ]
+        : [
+            "8",
+            "7",
+            "6",
+            "5",
+            "4",
+            "3",
+            "2",
+            "1"
+        ];
+
+
+    /*
+     * Coordenadas horizontales.
+     *
+     * Normal:
+     * a -> h
+     *
+     * Flip Board:
+     * h -> a
+     */
+    const fileCoordinates = flipped
+        ? [
+            "h",
+            "g",
+            "f",
+            "e",
+            "d",
+            "c",
+            "b",
+            "a"
+        ]
+        : [
+            "a",
+            "b",
+            "c",
+            "d",
+            "e",
+            "f",
+            "g",
+            "h"
+        ];
 
+
+    /*
+     * Flechas que finalmente
+     * renderizamos:
+     *
+     * - motor;
+     * - manuales.
+     */
+    const renderedArrows = useMemo(
+        () => [
+            ...(arrows ?? []).map(
+                arrow => ({
+                    ...arrow,
+
+                    colour:
+                        arrowStyle
+                            .suggestionColour
+                })
+            ),
+
+            ...manualArrows.map(
+                ([from, to]) => ({
+                    from,
+                    to,
+
+                    colour:
+                        arrowStyle
+                            .manualColour
+                })
+            )
+        ],
+        [
+            arrows,
+            manualArrows,
+            arrowStyle.suggestionColour,
+            arrowStyle.manualColour
+        ]
+    );
+
+
+    /*
+     * Perfiles superior e inferior
+     * dependiendo de Flip Board.
+     */
     const topProfile =
-        flipped ? whiteProfile : blackProfile;
+        flipped
+            ? whiteProfile
+            : blackProfile;
+
 
     const bottomProfile =
-        flipped ? blackProfile : whiteProfile;
+        flipped
+            ? blackProfile
+            : whiteProfile;
 
 
     function onSquareClick(
         square: Square,
         piece?: Piece
     ) {
+
         squares.setHighlighted([]);
 
+
         if (
             !piece
             || square == squares.selected
         ) {
-            squares.setSelected(undefined);
+
+            squares.setSelected(
+                undefined
+            );
+
             squares.clearPlayable();
+
         } else {
-            squares.setSelected(square);
+
+            squares.setSelected(
+                square
+            );
+
 
             squares.loadPlayable(
                 node.state.fen,
@@ -137,34 +353,56 @@ function Board({
             );
         }
 
+
         if (!squares.selected) {
             return;
         }
 
+
         if (
-            !squares.playable.includes(square)
-            && !squares.capturable.includes(square)
+            !squares.playable.includes(
+                square
+            )
+
+            &&
+
+            !squares.capturable.includes(
+                square
+            )
         ) {
             return;
         }
 
+
         const selectedPiece =
-            new Chess(node.state.fen)
-                .get(squares.selected);
+            new Chess(
+                node.state.fen
+            ).get(
+                squares.selected
+            );
+
 
         if (
             selectedPiece
-            && isMovePromotion(
+
+            &&
+
+            isMovePromotion(
                 selectedPiece.type,
                 square
             )
         ) {
+
             setHeldPromotion({
-                from: squares.selected,
-                to: square
+                from:
+                    squares.selected,
+
+                to:
+                    square
             });
         }
 
+
         addMove(
             squares.selected,
             square
@@ -177,6 +415,7 @@ function Board({
         from?: Square,
         to?: Square
     ) {
+
         if (
             !piece
             || !to
@@ -184,15 +423,22 @@ function Board({
             return false;
         }
 
-        setHeldPromotion(undefined);
+
+        setHeldPromotion(
+            undefined
+        );
+
 
         const fromSquare =
-            heldPromotion?.from || from;
+            heldPromotion?.from
+            || from;
+
 
         if (!fromSquare) {
             return false;
         }
 
+
         return addMove(
             fromSquare,
             to,
@@ -207,21 +453,31 @@ function Board({
         promotion?: PieceSymbol,
         drop?: boolean
     ) {
+
         try {
+
             const move =
-                new Chess(node.state.fen)
-                    .move({
-                        from,
-                        to,
-                        promotion
-                    });
+                new Chess(
+                    node.state.fen
+                ).move({
+                    from,
+                    to,
+                    promotion
+                });
+
 
             squares.setPieceDropFlag(
                 drop || false
             );
 
-            return onAddMove?.(move) || true;
+
+            return (
+                onAddMove?.(move)
+                || true
+            );
+
         } catch {
+
             return false;
         }
     }
@@ -229,66 +485,203 @@ function Board({
 
     return (
         <div
-            className={`${styles.wrapper} ${className}`}
-            style={style}
+            className={
+                `${styles.wrapper} ${className}`
+            }
+
+            style={
+                style
+            }
         >
+
+            {/*
+             * PERFIL SUPERIOR
+             */}
             {topProfile && (
                 <div
                     className={
                         `${styles.profile} ${profileClassName}`
                     }
+
                     style={{
-                        borderRadius: "7px 7px 0 0",
+                        borderRadius:
+                            "7px 7px 0 0",
+
                         ...profileStyle
                     }}
                 >
+
                     <PlayerProfile
-                        profile={topProfile}
+                        profile={
+                            topProfile
+                        }
                     />
+
                 </div>
             )}
 
+
+            {/*
+             * CONTENEDOR DEL TABLERO.
+             *
+             * Cuando las coordenadas
+             * est├ín fuera dejamos 22 px
+             * debajo para las letras a-h.
+             *
+             * IMPORTANTE:
+             * esto NO cambia la geometr├¡a
+             * interna del Chessboard.
+             */}
             <div
-                className={styles.boardContainer}
-                ref={boardContainerRef}
+                className={
+                    styles.boardContainer
+                }
+
+                ref={
+                    boardContainerRef
+                }
+
+                style={{
+                    marginBottom:
+                        coordinatesOutside
+                            ? "22px"
+                            : undefined
+                }}
             >
+
+                {/*
+                 * BARRA DE EVALUACI├ôN
+                 */}
                 {evaluation && (
                     <EvaluationBar
-                        evaluation={evaluation}
-                        moveColour={node.state.moveColour}
-                        flipped={flipped}
+                        evaluation={
+                            evaluation
+                        }
+
+                        moveColour={
+                            node.state.moveColour
+                        }
+
+                        flipped={
+                            flipped
+                        }
+
+                        style={{
+                            width:
+                                EVALUATION_BAR_WIDTH,
+
+                            minWidth:
+                                EVALUATION_BAR_WIDTH,
+
+                            marginRight:
+                                evaluationBarGap
+                        }}
                     />
                 )}
 
-                <SquaresContext.Provider value={squares}>
+
+                <SquaresContext.Provider
+                    value={
+                        squares
+                    }
+                >
+
+                    {/*
+                     * ESTE WRAPPER SIGUE SIENDO
+                     * EL MISMO DE ANTES.
+                     *
+                     * No Grid.
+                     * No nuevo contenedor.
+                     *
+                     * Chessboard conserva
+                     * exactamente su posici├│n.
+                     */}
                     <div
-                        className={styles.chessboardWrapper}
+                        className={
+                            styles
+                                .chessboardWrapper
+                        }
+
                         style={{
-                            width: chessboardWidth,
-                            height: chessboardWidth
+                            width:
+                                chessboardWidth,
+
+                            height:
+                                chessboardWidth
                         }}
                     >
+
+                        {/*
+                         * TABLERO REAL
+                         */}
                         <Chessboard
-                            position={node.state.fen}
+                            position={
+                                node.state.fen
+                            }
+
                             boardOrientation={
                                 flipped
                                     ? "black"
                                     : "white"
                             }
 
-                            customArrowColor="rgba(0,0,0,0)"
-                            onArrowsChange={setManualArrows}
 
-                            onSquareClick={onSquareClick}
+                            /*
+                             * INSIDE:
+                             *
+                             * react-chessboard
+                             * dibuja a-h / 1-8.
+                             *
+                             * OUTSIDE:
+                             *
+                             * react-chessboard
+                             * oculta las suyas.
+                             */
+                            showBoardNotation={
+                                !coordinatesOutside
+                            }
+
+
+                            /*
+                             * Ocultamos las flechas
+                             * originales de
+                             * react-chessboard.
+                             */
+                            customArrowColor={
+                                "rgba(0,0,0,0)"
+                            }
+
+
+                            /*
+                             * Pero seguimos usando
+                             * su detecci├│n de flechas
+                             * manuales.
+                             */
+                            onArrowsChange={
+                                setManualArrows
+                            }
+
+
+                            onSquareClick={
+                                onSquareClick
+                            }
+
+
                             onSquareRightClick={
-                                squares.toggleHighlight
+                                squares
+                                    .toggleHighlight
                             }
 
+
                             onPieceDragBegin={(
                                 piece,
                                 square
                             ) => {
-                                squares.setSelected(square);
+
+                                squares.setSelected(
+                                    square
+                                );
+
 
                                 squares.loadPlayable(
                                     node.state.fen,
@@ -296,96 +689,259 @@ function Board({
                                 );
                             }}
 
+
                             onPieceDrop={(
                                 from,
                                 to,
                                 piece
                             ) => {
-                                squares.setSelected(undefined);
-                                squares.clearPlayable();
+
+                                squares.setSelected(
+                                    undefined
+                                );
+
+
+                                squares
+                                    .clearPlayable();
+
 
                                 return addMove(
                                     from,
                                     to,
-                                    getPieceType(piece),
+                                    getPieceType(
+                                        piece
+                                    ),
                                     true
                                 );
                             }}
 
+
                             onPromotionPieceSelect={
                                 onPromotionPieceSelect
                             }
 
-                            customSquare={squareRenderer}
+
+                            customSquare={
+                                squareRenderer
+                            }
+
 
                             arePiecesDraggable={
                                 piecesDraggable
                             }
 
+
                             customLightSquareStyle={
-                                theme?.lightSquareColour
+                                theme
+                                    ?.lightSquareColour
+
                                     ? {
                                         backgroundColor:
-                                            theme.lightSquareColour
+                                            theme
+                                                .lightSquareColour
                                     }
+
                                     : undefined
                             }
 
+
                             customDarkSquareStyle={
-                                theme?.darkSquareColour
+                                theme
+                                    ?.darkSquareColour
+
                                     ? {
                                         backgroundColor:
-                                            theme.darkSquareColour
+                                            theme
+                                                .darkSquareColour
                                     }
+
                                     : undefined
                             }
 
-                            animationDuration={165}
+
+                            animationDuration={
+                                165
+                            }
+
 
                             showPromotionDialog={
                                 !!heldPromotion
                             }
 
+
                             promotionToSquare={
                                 heldPromotion?.to
                             }
 
-                            promotionDialogVariant="vertical"
 
-                            boardWidth={chessboardWidth}
+                            promotionDialogVariant={
+                                "vertical"
+                            }
+
+
+                            /*
+                             * IMPORTANTE:
+                             *
+                             * Seguimos enviando
+                             * directamente el tama├▒o
+                             * calculado al Chessboard.
+                             *
+                             * No hay ning├║n Grid
+                             * afect├índolo.
+                             */
+                            boardWidth={
+                                chessboardWidth
+                            }
                         />
 
+
+                        {/*
+                         * NUESTRO RENDERER
+                         * DE FLECHAS.
+                         */}
                         <SuggestionArrowOverlay
-                            arrows={renderedArrows}
-                            flipped={flipped}
-                            boardPixelWidth={chessboardWidth}
-                            shaftWidthPx={arrowStyle.width}
+                            arrows={
+                                renderedArrows
+                            }
+
+                            flipped={
+                                flipped
+                            }
+
+                            boardPixelWidth={
+                                chessboardWidth
+                            }
+
+                            shaftWidthPx={
+                                arrowStyle.width
+                            }
+
                             headLengthPx={
-                                arrowStyle.headLength
+                                arrowStyle
+                                    .headLength
                             }
+
                             headWidthPx={
-                                arrowStyle.headWidth
+                                arrowStyle
+                                    .headWidth
                             }
                         />
+
+
+                        {/*
+                         * COORDENADAS EXTERIORES.
+                         *
+                         * Se renderizan DENTRO
+                         * del chessboardWrapper,
+                         * pero mediante CSS:
+                         *
+                         * position: absolute.
+                         *
+                         * Por tanto NO participan
+                         * en el tama├▒o del tablero.
+                         */}
+                        {coordinatesOutside && (
+                            <>
+
+                                {/*
+                                 * N├ÜMEROS 8-1
+                                 */}
+                                <div
+                                    className={
+                                        styles
+                                            .rankCoordinates
+                                    }
+                                >
+
+                                    {rankCoordinates.map(
+                                        rank => (
+
+                                            <span
+                                                key={
+                                                    rank
+                                                }
+
+                                                className={
+                                                    styles
+                                                        .coordinate
+                                                }
+                                            >
+                                                {rank}
+                                            </span>
+
+                                        )
+                                    )}
+
+                                </div>
+
+
+                                {/*
+                                 * LETRAS A-H
+                                 */}
+                                <div
+                                    className={
+                                        styles
+                                            .fileCoordinates
+                                    }
+                                >
+
+                                    {fileCoordinates.map(
+                                        file => (
+
+                                            <span
+                                                key={
+                                                    file
+                                                }
+
+                                                className={
+                                                    styles
+                                                        .coordinate
+                                                }
+                                            >
+                                                {file}
+                                            </span>
+
+                                        )
+                                    )}
+
+                                </div>
+
+                            </>
+                        )}
+
                     </div>
+
                 </SquaresContext.Provider>
+
             </div>
 
+
+            {/*
+             * PERFIL INFERIOR
+             */}
             {bottomProfile && (
                 <div
                     className={
                         `${styles.profile} ${profileClassName}`
                     }
+
                     style={{
-                        borderRadius: "0 0 7px 7px",
+                        borderRadius:
+                            "0 0 7px 7px",
+
                         ...profileStyle
                     }}
                 >
+
                     <PlayerProfile
-                        profile={bottomProfile}
+                        profile={
+                            bottomProfile
+                        }
                     />
+
                 </div>
             )}
+
         </div>
     );
 }
diff --git a/client/src/apps/features/analysis/components/EvaluationBar/EvaluationBar.module.css b/client/src/apps/features/analysis/components/EvaluationBar/EvaluationBar.module.css
index c249486..57acb39 100644
--- a/client/src/apps/features/analysis/components/EvaluationBar/EvaluationBar.module.css
+++ b/client/src/apps/features/analysis/components/EvaluationBar/EvaluationBar.module.css
@@ -1,19 +1,145 @@
 .evaluationBar {
     position: relative;
-    min-width: 40px;
+
+    /*
+     * No dejamos que Flexbox
+     * comprima la barra.
+     */
+    flex-shrink: 0;
+
+    /*
+     * Las dos zonas quedan
+     * contenidas dentro
+     * de la propia barra.
+     */
+    overflow: hidden;
+
+    /*
+     * Un redondeo muy peque├▒o.
+     *
+     * No queremos que parezca
+     * un bot├│n o una tarjeta.
+     */
+    border-radius: 2px;
+
+    /*
+     * L├¡nea interior extremadamente
+     * tenue para delimitar la barra
+     * sobre fondos oscuros.
+     */
+    box-shadow:
+        inset 0 0 0 1px
+        rgba(0, 0, 0, 0.12);
 }
 
+
+/*
+ * Zona animada de la barra.
+ *
+ * Esta capa ocupa f├¡sicamente
+ * siempre el 100%.
+ *
+ * Lo ├║nico que cambia es
+ * su escala vertical.
+ *
+ * Por ejemplo:
+ *
+ * scaleY(0.50)
+ * scaleY(0.63)
+ * scaleY(0.21)
+ *
+ * Mucho m├ís eficiente que
+ * animar directamente "height".
+ */
 .overBar {
+    position: absolute;
+
+    top: 0;
+    left: 0;
+
     width: 100%;
+    height: 100%;
+
+    /*
+     * La barra crece/decrece
+     * desde arriba.
+     */
+    transform-origin:
+        top center;
 
-    transition: height 0.5s ease-out;
+    /*
+     * Animaci├│n suave.
+     *
+     * Empieza r├ípida pero
+     * desacelera de forma limpia.
+     */
+    transition:
+        transform
+        500ms
+        cubic-bezier(
+            0.16,
+            1,
+            0.3,
+            1
+        );
+
+    /*
+     * Le indicamos al navegador
+     * que este elemento va
+     * a cambiar su transform.
+     *
+     * Esto favorece que la
+     * animaci├│n se gestione
+     * mediante composici├│n GPU.
+     */
+    will-change: transform;
 }
 
+
+/*
+ * N├║mero:
+ *
+ * 4.7
+ * 1.8
+ * etc.
+ */
 .evaluationText {
+    position: absolute;
+
+    left: 0;
+
+    z-index: 2;
+
     display: flex;
-    justify-content: center;
 
-    position: absolute;
+    justify-content: center;
+    align-items: center;
 
     width: 100%;
+
+    /*
+     * La barra es bastante fina,
+     * as├¡ que usamos una fuente
+     * compacta.
+     */
+    font-size: 0.68rem;
+
+    font-weight: 700;
+
+    line-height: 1;
+
+    /*
+     * El texto nunca debe
+     * bloquear clics.
+     */
+    pointer-events: none;
+
+    /*
+     * Peque├▒a transici├│n cuando
+     * cambia de lado/color.
+     */
+    transition:
+        color
+        200ms
+        ease;
 }
\ No newline at end of file
diff --git a/client/src/apps/features/analysis/components/EvaluationBar/index.tsx b/client/src/apps/features/analysis/components/EvaluationBar/index.tsx
index 0a33bcd..3f9297b 100644
--- a/client/src/apps/features/analysis/components/EvaluationBar/index.tsx
+++ b/client/src/apps/features/analysis/components/EvaluationBar/index.tsx
@@ -2,10 +2,24 @@ import React, { useMemo } from "react";
 import { clamp } from "lodash-es";
 
 import PieceColour from "shared/constants/PieceColour";
-import { stringifyEvaluation } from "shared/lib/utils/chess";
 
-import EvaluationBarProps from "./EvaluationBarProps";
-import * as styles from "./EvaluationBar.module.css";
+import {
+    stringifyEvaluation
+} from "shared/lib/utils/chess";
+
+import EvaluationBarProps from
+    "./EvaluationBarProps";
+
+import * as styles from
+    "./EvaluationBar.module.css";
+
+
+const WHITE_COLOUR = "#FFFFFF";
+
+const BLACK_COLOUR = "#403D39";
+
+const WHITE_EVALUATION_TEXT_COLOUR =
+    "#302E2B";
 
 
 function EvaluationBar({
@@ -15,87 +29,283 @@ function EvaluationBar({
     moveColour,
     flipped = false
 }: EvaluationBarProps) {
+
+    /*
+     * Texto que aparece dentro
+     * de la barra.
+     *
+     * Queremos siempre valor absoluto.
+     *
+     * +4.73 -> 4.7
+     * -1.82 -> 1.8
+     *
+     * El signo positivo/negativo
+     * se conserva ├║nicamente
+     * en el gr├ífico de evaluaci├│n.
+     */
     const evaluationText = useMemo(() => (
-        stringifyEvaluation({
-            ...evaluation,
-            value: Math.abs(evaluation.value)
-        }, false, 1)
-    ), [evaluation]);
-
-    const overBarHeight = useMemo(() => {
-        if (evaluation.type == "centipawn") {
+        stringifyEvaluation(
+            {
+                ...evaluation,
+
+                value: Math.abs(
+                    evaluation.value
+                )
+            },
+
+            false,
+
+            1
+        )
+    ), [
+        evaluation
+    ]);
+
+
+    /*
+     * Calculamos qu├® porcentaje
+     * de la barra pertenece
+     * visualmente a las negras.
+     *
+     * 50%:
+     * posici├│n igualada.
+     *
+     * Menos de 50%:
+     * ventaja blanca.
+     *
+     * M├ís de 50%:
+     * ventaja negra.
+     *
+     * Conservamos la f├│rmula
+     * que utilizaba WintrChess
+     * para no cambiar todav├¡a
+     * el comportamiento matem├ítico
+     * de la evaluaci├│n.
+     */
+    const blackPercentage = useMemo(() => {
+
+        /*
+         * Evaluaci├│n normal
+         * en centipawns.
+         */
+        if (
+            evaluation.type
+            == "centipawn"
+        ) {
             return clamp(
-                50 - (evaluation.value / 20),
+                50
+                - (
+                    evaluation.value
+                    / 20
+                ),
+
                 5,
+
                 95
             );
         }
 
-        return evaluation.value == 0
-            ? (
-                moveColour == PieceColour.WHITE
-                    ? 0
-                    : 100
+
+        /*
+         * Evaluaci├│n de mate.
+         */
+        if (
+            evaluation.value == 0
+        ) {
+            return (
+                moveColour
+                == PieceColour.WHITE
             )
-            : (
-                evaluation.value > 0
-                    ? 0
-                    : 100
-            );
+                ? 0
+                : 100;
+        }
+
+
+        return (
+            evaluation.value > 0
+        )
+            ? 0
+            : 100;
+
     }, [
         evaluation,
         moveColour
     ]);
 
-    const textBottom =
-        overBarHeight > 50 == flipped;
+
+    /*
+     * Convertimos el porcentaje
+     * a una escala entre 0 y 1.
+     *
+     * Ejemplo:
+     *
+     * 63%
+     * ->
+     * 0.63
+     *
+     * Esto se utilizar├í con:
+     *
+     * transform: scaleY(...)
+     *
+     * en lugar de cambiar height.
+     */
+    const blackRatio =
+        blackPercentage / 100;
+
+
+    /*
+     * Cuando el tablero est├í normal:
+     *
+     * negras arriba
+     * blancas abajo
+     *
+     * El fondo de la barra es blanco
+     * y dibujamos una capa negra
+     * creciendo desde arriba.
+     *
+     *
+     * Cuando Flip Board est├í activo:
+     *
+     * blancas arriba
+     * negras abajo
+     *
+     * El fondo pasa a ser negro
+     * y dibujamos la parte blanca
+     * creciendo desde arriba.
+     */
+    const overlayRatio = flipped
+        ? 1 - blackRatio
+        : blackRatio;
+
+
+    const baseColour = flipped
+        ? BLACK_COLOUR
+        : WHITE_COLOUR;
+
+
+    const overlayColour = flipped
+        ? WHITE_COLOUR
+        : BLACK_COLOUR;
+
+
+    /*
+     * Determinamos qui├®n tiene
+     * la ventaja.
+     *
+     * Si la zona negra ocupa
+     * menos de la mitad,
+     * ganan blancas.
+     *
+     * Si ocupa m├ís,
+     * ganan negras.
+     */
+    const whiteIsBetter =
+        blackPercentage <= 50;
+
+
+    /*
+     * Posici├│n del n├║mero.
+     *
+     * Tablero normal:
+     *
+     * Blancas ganan -> abajo.
+     * Negras ganan  -> arriba.
+     *
+     * Cuando el tablero se gira,
+     * la etiqueta sigue visualmente
+     * al color correspondiente.
+     */
+    const evaluationAtBottom = flipped
+        ? !whiteIsBetter
+        : whiteIsBetter;
+
+
+    /*
+     * Color del n├║mero.
+     *
+     * Ventaja blanca:
+     * texto oscuro.
+     *
+     * Ventaja negra:
+     * texto blanco.
+     */
+    const evaluationTextColour =
+        whiteIsBetter
+            ? WHITE_EVALUATION_TEXT_COLOUR
+            : WHITE_COLOUR;
+
 
     return (
         <div
             className={
                 `${styles.evaluationBar} ${className}`
             }
+
             style={{
                 backgroundColor:
-                    flipped
-                        ? "#0c0c0c"
-                        : "#fff",
+                    baseColour,
 
                 ...style
             }}
         >
+
+            {/*
+             * CAPA SUPERIOR ANIMADA.
+             *
+             * Muy importante:
+             *
+             * Ya NO modificamos:
+             *
+             * height: 63%
+             *
+             * Ahora usamos:
+             *
+             * transform: scaleY(0.63)
+             *
+             * Esto evita recalcular
+             * constantemente el layout
+             * durante la animaci├│n.
+             */}
             <div
-                className={styles.overBar}
+                className={
+                    styles.overBar
+                }
+
                 style={{
                     backgroundColor:
-                        flipped
-                            ? "#fff"
-                            : "#0c0c0c",
-
-                    height:
-                        flipped
-                            ? `calc(100% - ${overBarHeight}%)`
-                            : `${overBarHeight}%`
+                        overlayColour,
+
+                    transform:
+                        `scaleY(${overlayRatio})`
                 }}
             />
 
+
+            {/*
+             * N├ÜMERO DE EVALUACI├ôN.
+             */}
             <span
-                className={styles.evaluationText}
+                className={
+                    styles.evaluationText
+                }
+
                 style={{
-                    [
-                        textBottom
-                            ? "bottom"
-                            : "top"
-                    ]: "7px",
+                    ...(evaluationAtBottom
+                        ? {
+                            bottom: "6px"
+                        }
+                        : {
+                            top: "6px"
+                        }
+                    ),
 
                     color:
-                        overBarHeight > 50
-                            ? "#fff"
-                            : "#000"
+                        evaluationTextColour
                 }}
             >
                 {evaluationText}
             </span>
+
         </div>
     );
 }
diff --git a/client/src/apps/features/analysis/components/report/EvaluationGraph/EvaluationGraph.module.css b/client/src/apps/features/analysis/components/report/EvaluationGraph/EvaluationGraph.module.css
index 6435a60..7546525 100644
--- a/client/src/apps/features/analysis/components/report/EvaluationGraph/EvaluationGraph.module.css
+++ b/client/src/apps/features/analysis/components/report/EvaluationGraph/EvaluationGraph.module.css
@@ -1,36 +1,141 @@
 .wrapper {
     display: flex;
+
     flex-direction: column;
+
     gap: 5px;
+
     padding: 5px;
-    border-radius: 10px;
-    background-color: var(--ui-shade-4);
+
+    border-radius: 8px;
+
+    background-color:
+        var(--ui-shade-4);
 }
 
+
+/*
+ * Superficie principal
+ * del gr├ífico.
+ */
 .chart {
-    border-radius: 10px;
-    background-color: #1f1f1f;
+    border-radius: 8px;
+
+    background-color:
+        #1f1f1f;
+
+    cursor:
+        pointer !important;
 
-    cursor: pointer !important;
-    overflow: hidden;
+    overflow:
+        hidden;
 }
 
+
+/*
+ * TOOLTIP
+ *
+ * Peque├▒o, limpio
+ * y muy ligero.
+ */
 .tooltip {
     display: flex;
+
     flex-direction: column;
-    padding: 5px;
 
-    border: 1px solid black;
-    border-radius: 5px;
-    background-color: rgba(255, 255, 255, 0.75);
+    align-items: flex-start;
+
+    gap: 2px;
+
+    min-width: 52px;
+
+    padding:
+        6px 9px;
+
+    border:
+        1px solid
+        rgba(0, 0, 0, 0.25);
+
+    border-radius:
+        4px;
+
+    background-color:
+        rgba(
+            245,
+            245,
+            245,
+            0.95
+        );
+
+    color:
+        #403D39;
+
+    box-shadow:
+        0 2px 7px
+        rgba(
+            0,
+            0,
+            0,
+            0.18
+        );
+
+    pointer-events:
+        none;
 }
 
+
+/*
+ * Evaluaci├│n principal.
+ *
+ * Ejemplo:
+ *
+ * +2.28
+ * -1.83
+ */
 .tooltipEvaluation {
-    display: flex;
-    align-items: center;
-    gap: 5px;
+    font-size:
+        0.82rem;
+
+    font-weight:
+        700;
+
+    line-height:
+        1.1;
+
+    color:
+        #403D39;
+
+    white-space:
+        nowrap;
 }
 
+
+/*
+ * Movimiento.
+ *
+ * Ejemplo:
+ *
+ * Nxe5
+ * Qxd3
+ */
 .tooltipMove {
-    font-size: 0.9rem;
+    font-size:
+        0.7rem;
+
+    font-weight:
+        500;
+
+    line-height:
+        1.1;
+
+    color:
+        rgba(
+            64,
+            61,
+            57,
+            0.75
+        );
+
+    white-space:
+        nowrap;
 }
\ No newline at end of file
diff --git a/client/src/apps/features/analysis/components/report/EvaluationGraph/TooltipRenderer.tsx b/client/src/apps/features/analysis/components/report/EvaluationGraph/TooltipRenderer.tsx
index 2fa4b2a..a4ba1f8 100644
--- a/client/src/apps/features/analysis/components/report/EvaluationGraph/TooltipRenderer.tsx
+++ b/client/src/apps/features/analysis/components/report/EvaluationGraph/TooltipRenderer.tsx
@@ -1,36 +1,93 @@
 import React from "react";
 
-import { stringifyEvaluation } from "shared/lib/utils/chess";
-import { classificationImages } from "@analysis/constants/classifications";
+import {
+    stringifyEvaluation
+} from "shared/lib/utils/chess";
+
+import EvaluationGraphPoint from
+    "./Point";
+
+import * as styles from
+    "./EvaluationGraph.module.css";
 
-import EvaluationGraphPoint from "./Point";
-import * as styles from "./EvaluationGraph.module.css";
 
 interface TooltipRendererProps {
-    dataPoint: EvaluationGraphPoint;
+
+    dataPoint:
+        EvaluationGraphPoint;
+
 }
 
-function TooltipRenderer({ dataPoint }: TooltipRendererProps) {
-    return <div className={styles.tooltip}>
-        <div className={styles.tooltipEvaluation}>
-            {dataPoint.state.classification
-                && <img
-                    src={classificationImages[dataPoint.state.classification]}
-                    height={25}
-                />
+
+function TooltipRenderer({
+    dataPoint
+}: TooltipRendererProps) {
+
+    return (
+        <div
+            className={
+                styles.tooltip
             }
+        >
 
-            <span>
-                {stringifyEvaluation(dataPoint.evaluation, true)}
-            </span>
-        </div>
+            {/*
+             * Evaluaci├│n:
+             *
+             * blancas:
+             * +2.28
+             *
+             * negras:
+             * -1.83
+             *
+             * Conservamos dos decimales
+             * y el signo.
+             */}
+            <span
+                className={
+                    styles.tooltipEvaluation
+                }
+            >
+
+                {stringifyEvaluation(
+                    dataPoint.evaluation,
+                    true
+                )}
 
-        {dataPoint.state.move?.san
-            && <span className={styles.tooltipMove}>
-                {dataPoint.state.move?.san}
             </span>
-        }
-    </div>;
+
+
+            {/*
+             * Movimiento correspondiente:
+             *
+             * Nxe5
+             * Qxd3
+             * O-O
+             * etc.
+             *
+             * Solo aparece si existe.
+             */}
+            {dataPoint.state.move?.san && (
+
+                <span
+                    className={
+                        styles.tooltipMove
+                    }
+                >
+
+                    {
+                        dataPoint
+                            .state
+                            .move
+                            .san
+                    }
+
+                </span>
+
+            )}
+
+        </div>
+    );
 }
 
+
 export default TooltipRenderer;
\ No newline at end of file
diff --git a/client/src/apps/features/analysis/components/report/EvaluationGraph/index.tsx b/client/src/apps/features/analysis/components/report/EvaluationGraph/index.tsx
index e63fcb4..6744734 100644
--- a/client/src/apps/features/analysis/components/report/EvaluationGraph/index.tsx
+++ b/client/src/apps/features/analysis/components/report/EvaluationGraph/index.tsx
@@ -1,4 +1,5 @@
 import React from "react";
+
 import {
     ResponsiveContainer,
     XAxis,
@@ -9,50 +10,132 @@ import {
     ReferenceLine,
     ReferenceDot
 } from "recharts";
+
 import { max } from "lodash-es";
 
-import { StateTreeNode } from "shared/types/game/position/StateTreeNode";
-import Evaluation from "shared/types/game/position/Evaluation";
-import { defaultEvaluation } from "shared/constants/utils";
-import PieceColour from "shared/constants/PieceColour";
-import { Classification } from "shared/constants/Classification";
-import { getTopEngineLine } from "shared/types/game/position/EngineLine";
-import { classificationColours } from "@analysis/constants/classifications";
+import {
+    StateTreeNode
+} from "shared/types/game/position/StateTreeNode";
+
+import Evaluation from
+    "shared/types/game/position/Evaluation";
+
+import {
+    defaultEvaluation
+} from "shared/constants/utils";
+
+import PieceColour from
+    "shared/constants/PieceColour";
+
+import {
+    Classification
+} from "shared/constants/Classification";
+
+import {
+    getTopEngineLine
+} from "shared/types/game/position/EngineLine";
+
+import {
+    classificationColours
+} from "@analysis/constants/classifications";
 
 import EvaluationGraphPoint from "./Point";
-import TooltipRenderer from "./TooltipRenderer";
-import EvaluationGraphProps from "./EvaluationGraphProps";
-import * as styles from "./EvaluationGraph.module.css";
-
-const highlightedClassifications: Classification[] = [
-    Classification.BRILLIANT,
-    Classification.CRITICAL,
-    Classification.INACCURACY,
-    Classification.MISTAKE,
-    Classification.BLUNDER
-];
 
+import TooltipRenderer from
+    "./TooltipRenderer";
+
+import EvaluationGraphProps from
+    "./EvaluationGraphProps";
+
+import * as styles from
+    "./EvaluationGraph.module.css";
+
+
+/*
+ * Clasificaciones que aparecen
+ * destacadas como puntos de color
+ * en el gr├ífico.
+ */
+const highlightedClassifications:
+    Classification[] = [
+
+        Classification.BRILLIANT,
+
+        Classification.CRITICAL,
+
+        Classification.INACCURACY,
+
+        Classification.MISTAKE,
+
+        Classification.BLUNDER
+    ];
+
+
+/*
+ * Convierte una evaluaci├│n real
+ * del motor en la posici├│n vertical
+ * utilizada por el gr├ífico.
+ */
 function getGraphY(
     node: StateTreeNode,
     evaluation: Evaluation,
     graphHeight: number
 ) {
-    if (evaluation.type == "mate") {
-        if (evaluation.value == 0) {
-            if (node.state.moveColour == undefined) {
-                return graphHeight / 2;
+
+    /*
+     * Posiciones de mate.
+     */
+    if (
+        evaluation.type
+        == "mate"
+    ) {
+
+        if (
+            evaluation.value
+            == 0
+        ) {
+
+            if (
+                node.state.moveColour
+                == undefined
+            ) {
+
+                return (
+                    graphHeight / 2
+                );
             }
 
-            return node.state.moveColour == PieceColour.WHITE
-                ? graphHeight : 0;
+
+            return (
+                node.state.moveColour
+                == PieceColour.WHITE
+            )
+                ? graphHeight
+                : 0;
         }
 
-        return evaluation.value >= 0 ? graphHeight : 0;
+
+        return (
+            evaluation.value >= 0
+        )
+            ? graphHeight
+            : 0;
     }
 
-    return evaluation.value + (graphHeight / 2);
+
+    /*
+     * Evaluaci├│n normal
+     * en centipawns.
+     */
+    return (
+        evaluation.value
+        + (
+            graphHeight / 2
+        )
+    );
 }
 
+
 function EvaluationGraph({
     className,
     style,
@@ -60,115 +143,430 @@ function EvaluationGraph({
     selectedIndex,
     onPointClick
 }: EvaluationGraphProps) {
+
+    /*
+     * Buscamos la evaluaci├│n absoluta
+     * m├ís extrema de toda la partida.
+     *
+     * Se utiliza para dimensionar
+     * verticalmente el gr├ífico.
+     */
     const absoluteHighestValue = max(
-        nodes.map(node => Math.abs(
-            getTopEngineLine(node.state.engineLines)?.evaluation.value || 0
-        ))
+
+        nodes.map(
+            node => Math.abs(
+
+                getTopEngineLine(
+                    node.state.engineLines
+                )?.evaluation.value
+
+                || 0
+            )
+        )
+
     ) || 0;
 
-    const yAxisPadding = absoluteHighestValue * 0.2;
 
-    const dataPoints = nodes.map((node, index) => {
-        const evaluation = getTopEngineLine(node.state.engineLines)?.evaluation
-            || defaultEvaluation;
+    /*
+     * Dejamos algo de margen vertical
+     * para que los picos no queden
+     * totalmente pegados al borde.
+     */
+    const yAxisPadding =
+        absoluteHighestValue
+        * 0.2;
 
-        const graphHeight = (absoluteHighestValue + yAxisPadding) * 2;
 
-        return {
-            nodeId: node.id,
-            state: node.state,
-            evaluation: evaluation,
-            x: index,
-            y: getGraphY(node, evaluation, graphHeight)
-        } as EvaluationGraphPoint;
-    });
+    /*
+     * Convertimos cada posici├│n
+     * de la partida en un punto
+     * del gr├ífico.
+     */
+    const dataPoints = nodes.map(
+        (
+            node,
+            index
+        ) => {
 
-    const highlightedPoints = dataPoints.filter(point => (
-        point.state.classification
-        && highlightedClassifications.includes(
-            point.state.classification
-        )
-    ));
+            const evaluation =
+                getTopEngineLine(
+                    node.state.engineLines
+                )?.evaluation
+
+                || defaultEvaluation;
 
-    const selectedPoint = dataPoints[selectedIndex];
 
-    const selectedPointColour = selectedPoint?.state.classification
-        ? classificationColours[selectedPoint.state.classification]
-        : "gray";
+            const graphHeight = (
+                absoluteHighestValue
+                + yAxisPadding
+            ) * 2;
 
-    return <div className={styles.wrapper}>
-        <ResponsiveContainer
-            width={style?.width || "100%"}
-            height={style?.height || 100}
+
+            return {
+
+                nodeId:
+                    node.id,
+
+                state:
+                    node.state,
+
+                evaluation:
+                    evaluation,
+
+                x:
+                    index,
+
+                y:
+                    getGraphY(
+                        node,
+                        evaluation,
+                        graphHeight
+                    )
+
+            } as EvaluationGraphPoint;
+        }
+    );
+
+
+    /*
+     * Puntos con clasificaciones
+     * especialmente importantes.
+     */
+    const highlightedPoints =
+        dataPoints.filter(
+            point => (
+
+                point.state.classification
+
+                &&
+
+                highlightedClassifications.includes(
+                    point.state
+                        .classification
+                )
+            )
+        );
+
+
+    /*
+     * Movimiento actualmente
+     * seleccionado en la revisi├│n.
+     */
+    const selectedPoint =
+        dataPoints[
+            selectedIndex
+        ];
+
+
+    /*
+     * Color de la l├¡nea vertical
+     * del movimiento seleccionado.
+     *
+     * Si tiene clasificaci├│n:
+     * usamos su color.
+     *
+     * Si no:
+     * gris.
+     */
+    const selectedPointColour =
+        selectedPoint
+            ?.state
+            .classification
+
+            ? classificationColours[
+                selectedPoint
+                    .state
+                    .classification
+            ]
+
+            : "gray";
+
+
+    return (
+        <div
+            className={
+                styles.wrapper
+            }
         >
-            <AreaChart
-                className={`${styles.chart} ${className}`}
-                margin={{ top: 0, right: 0, bottom: 0, left: 0 }}
-                data={dataPoints}
-                onClick={event => {
-                    const payload = event.activePayload?.at(0)?.payload;
-                    if (!payload) return;
-
-                    onPointClick?.(payload as EvaluationGraphPoint);
-                }}
+
+            <ResponsiveContainer
+                width={
+                    style?.width
+                    || "100%"
+                }
+
+                height={
+                    style?.height
+                    || 100
+                }
             >
-                <XAxis hide dataKey="x"/>
-                <YAxis hide domain={[
-                    0, absoluteHighestValue * 2 + (yAxisPadding * 2)
-                ]}/>
-
-                <Area
-                    dataKey="y"
-                    type="monotone"
-                    fill="#fff"
-                    fillOpacity={1}
-                    strokeWidth={0}
-                    isAnimationActive={false}
-                />
-
-                <ReferenceLine
-                    y={absoluteHighestValue + yAxisPadding}
-                    stroke="gray"
-                    strokeOpacity={0.5}
-                    strokeWidth={2}
-                />
-
-                {selectedPoint && <>
-                    <ReferenceLine
-                        x={selectedPoint.x}
-                        stroke={selectedPointColour}
-                        strokeWidth={2}
+
+                <AreaChart
+                    className={
+                        `${styles.chart} ${className}`
+                    }
+
+                    margin={{
+                        top: 0,
+                        right: 0,
+                        bottom: 0,
+                        left: 0
+                    }}
+
+                    data={
+                        dataPoints
+                    }
+
+                    onClick={event => {
+    const payload =
+        event.activePayload?.at(0)?.payload;
+
+    if (!payload) {
+        return;
+    }
+
+    onPointClick?.(
+        payload as EvaluationGraphPoint
+    );
+}}
+                >
+
+                    {/*
+                     * Los ejes existen
+                     * internamente,
+                     * pero no se muestran.
+                     */}
+                    <XAxis
+                        hide
+                        dataKey="x"
                     />
 
-                    <ReferenceDot
-                        x={selectedPoint.x}
-                        y={selectedPoint.y}
-                        r={4}
-                        fill={selectedPointColour}
+
+                    <YAxis
+                        hide
+
+                        domain={[
+                            0,
+
+                            absoluteHighestValue
+                            * 2
+
+                            +
+
+                            (
+                                yAxisPadding
+                                * 2
+                            )
+                        ]}
+                    />
+
+
+                    {/*
+                     * ├ürea blanca principal
+                     * que representa
+                     * la evoluci├│n.
+                     */}
+                    <Area
+                        dataKey="y"
+
+                        type="monotone"
+
+                        fill="#FFFFFF"
+
+                        fillOpacity={1}
+
                         strokeWidth={0}
+
+                        isAnimationActive={
+                            false
+                        }
+                    />
+
+
+                    {/*
+                     * L├¡nea horizontal central:
+                     *
+                     * evaluaci├│n 0.00.
+                     */}
+                    <ReferenceLine
+                        y={
+                            absoluteHighestValue
+                            + yAxisPadding
+                        }
+
+                        stroke={
+                            "rgba(120, 120, 120, 0.55)"
+                        }
+
+                        strokeWidth={
+                            1
+                        }
+                    />
+
+
+                    {/*
+                     * TOOLTIP DEL CURSOR.
+                     *
+                     * Cuando movemos el rat├│n
+                     * sobre el gr├ífico:
+                     *
+                     * - aparece el tooltip;
+                     * - aparece una l├¡nea vertical;
+                     * - la l├¡nea es tenue;
+                     * - es discontinua.
+                     *
+                     * Esto NO sustituye
+                     * la l├¡nea del movimiento
+                     * seleccionado.
+                     */}
+                    <Tooltip
+                        cursor={{
+                            stroke:
+                                "rgba(255, 255, 255, 0.32)",
+
+                            strokeWidth:
+                                1,
+
+                            strokeDasharray:
+                                "4 4"
+                        }}
+
+                        content={({
+                            label
+                        }) => {
+
+                            const point =
+                                typeof label
+                                == "number"
+
+                                &&
+
+                                dataPoints[
+                                    label
+                                ];
+
+
+                            return point
+
+                                ? (
+                                    <TooltipRenderer
+                                        dataPoint={
+                                            point
+                                        }
+                                    />
+                                )
+
+                                : null;
+                        }}
                     />
-                </>}
-
-                {highlightedPoints.map(point => <ReferenceDot
-                    key={point.nodeId}
-                    x={point.x}
-                    y={point.y}
-                    r={3}
-                    fill={classificationColours[point.state.classification!]}
-                    strokeWidth={0}
-                />)}
-
-                <Tooltip content={({ label }) => {
-                    const point = typeof label == "number"
-                        && dataPoints[label];
-
-                    return point
-                        ? <TooltipRenderer dataPoint={point} />
-                        : null;
-                }}/>
-            </AreaChart>
-        </ResponsiveContainer>
-    </div>;
+
+
+                    {/*
+                     * MOVIMIENTO SELECCIONADO.
+                     *
+                     * L├¡nea s├│lida.
+                     *
+                     * Es independiente
+                     * de la l├¡nea discontinua
+                     * del cursor.
+                     */}
+                    {selectedPoint && (
+                        <>
+
+                            <ReferenceLine
+                                x={
+                                    selectedPoint.x
+                                }
+
+                                stroke={
+                                    selectedPointColour
+                                }
+
+                                strokeWidth={
+                                    2
+                                }
+                            />
+
+
+                            <ReferenceDot
+                                x={
+                                    selectedPoint.x
+                                }
+
+                                y={
+                                    selectedPoint.y
+                                }
+
+                                r={
+                                    4
+                                }
+
+                                fill={
+                                    selectedPointColour
+                                }
+
+                                strokeWidth={
+                                    0
+                                }
+                            />
+
+                        </>
+                    )}
+
+
+                    {/*
+                     * Puntos de clasificaciones
+                     * relevantes:
+                     *
+                     * brillante,
+                     * cr├¡tica,
+                     * imprecisi├│n,
+                     * error,
+                     * blunder.
+                     */}
+                    {highlightedPoints.map(
+                        point => (
+
+                            <ReferenceDot
+                                key={
+                                    point.nodeId
+                                }
+
+                                x={
+                                    point.x
+                                }
+
+                                y={
+                                    point.y
+                                }
+
+                                r={
+                                    3
+                                }
+
+                                fill={
+                                    classificationColours[
+                                        point
+                                            .state
+                                            .classification!
+                                    ]
+                                }
+
+                                strokeWidth={
+                                    0
+                                }
+                            />
+
+                        )
+                    )}
+
+                </AreaChart>
+
+            </ResponsiveContainer>
+
+        </div>
+    );
 }
 
+
 export default EvaluationGraph;
\ No newline at end of file
diff --git a/client/src/apps/features/analysis/constants/classifications.ts b/client/src/apps/features/analysis/constants/classifications.ts
index fb33046..188029f 100644
--- a/client/src/apps/features/analysis/constants/classifications.ts
+++ b/client/src/apps/features/analysis/constants/classifications.ts
@@ -23,6 +23,7 @@ export const classificationImages = {
     [Classification.OKAY]: iconClassificationsOkay,
     [Classification.INACCURACY]: iconClassificationsInaccuracy,
     [Classification.MISTAKE]: iconClassificationsMistake,
+    [Classification.MISS]: iconClassificationsError,
     [Classification.BLUNDER]: iconClassificationsBlunder,
     [Classification.FORCED]: iconClassificationsForced,
     [Classification.THEORY]: iconClassificationsTheory,
@@ -41,6 +42,7 @@ export const classificationColours = {
     [Classification.OKAY]: "#97af8b",
     [Classification.INACCURACY]: "#f4bf44",
     [Classification.MISTAKE]: "#e28c28",
+    [Classification.MISS]: "#ff6b6b",
     [Classification.BLUNDER]: "#c93230",
     [Classification.FORCED]: "#97af8b",
     [Classification.THEORY]: "#a88764",
@@ -55,6 +57,7 @@ export const classificationNames = {
     [Classification.OKAY]: "classifications.okay",
     [Classification.INACCURACY]: "classifications.inaccuracy",
     [Classification.MISTAKE]: "classifications.mistake",
+    [Classification.MISS]: "classifications.miss",
     [Classification.BLUNDER]: "classifications.blunder",
     [Classification.FORCED]: "classifications.forced",
     [Classification.THEORY]: "classifications.theory",
diff --git a/client/src/apps/features/analysis/hooks/useAnalyseGame.ts b/client/src/apps/features/analysis/hooks/useAnalyseGame.ts
index 7d4ef95..5a0ef55 100644
--- a/client/src/apps/features/analysis/hooks/useAnalyseGame.ts
+++ b/client/src/apps/features/analysis/hooks/useAnalyseGame.ts
@@ -33,11 +33,46 @@ function useAnalyseGame(
     const executeCaptcha = useAltcha();
 
     return async () => {
-        const analyseResult = await analyseStateTree(analysisGame.stateTree, {
-            includeBrilliant: settings.classifications.included.brilliant,
-            includeCritical: settings.classifications.included.critical,
-            includeTheory: settings.classifications.included.theory
-        });
+        const analyseResult = await analyseStateTree(
+    analysisGame.stateTree,
+    {
+        includeBrilliant:
+            settings
+                .classifications
+                .included
+                .brilliant,
+
+        includeCritical:
+            settings
+                .classifications
+                .included
+                .critical,
+
+        includeTheory:
+            settings
+                .classifications
+                .included
+                .theory,
+
+
+        /*
+         * Pasamos el rating
+         * de la modalidad contenido
+         * en el PGN/importaci├│n.
+         */
+        whiteRating:
+            analysisGame
+                .players
+                .white
+                .rating,
+
+        blackRating:
+            analysisGame
+                .players
+                .black
+                .rating
+    }
+);
 
         // For any errors, display message or reset CAPTCHA
         if (analyseResult.status == StatusCodes.UNAUTHORIZED) {
diff --git a/client/src/apps/features/analysis/lib/reporter.ts b/client/src/apps/features/analysis/lib/reporter.ts
index 6b4b3c5..488cee2 100644
--- a/client/src/apps/features/analysis/lib/reporter.ts
+++ b/client/src/apps/features/analysis/lib/reporter.ts
@@ -18,9 +18,43 @@ export async function analyseStateTree(
     options?: AnalysisOptions
 ): APIResponse<{ gameAnalysis: GameAnalysis }> {
     const reportURL = "/api/analysis/analyse"
-        + `?brilliant=${String(options?.includeBrilliant)}`
-        + `&critical=${String(options?.includeCritical)}`
-        + `&theory=${String(options?.includeTheory)}`;
+
+    + `?brilliant=${
+        String(
+            options
+                ?.includeBrilliant
+        )
+    }`
+
+    + `&critical=${
+        String(
+            options
+                ?.includeCritical
+        )
+    }`
+
+    + `&theory=${
+        String(
+            options
+                ?.includeTheory
+        )
+    }`
+
+    + `&whiteRating=${
+        String(
+            options
+                ?.whiteRating
+            ?? ""
+        )
+    }`
+
+    + `&blackRating=${
+        String(
+            options
+                ?.blackRating
+            ?? ""
+        )
+    }`;
 
     const reportResponse = await fetch(reportURL, {
         method: "POST",
diff --git a/client/src/apps/settings/components/categories/BoardAndPieces/BoardAndPieces.module.css b/client/src/apps/settings/components/categories/BoardAndPieces/BoardAndPieces.module.css
index eaa1f88..a706945 100644
--- a/client/src/apps/settings/components/categories/BoardAndPieces/BoardAndPieces.module.css
+++ b/client/src/apps/settings/components/categories/BoardAndPieces/BoardAndPieces.module.css
@@ -1,49 +1,7 @@
 .presets {
     display: flex;
-    flex-wrap: wrap;
-    gap: 10px;
-}
-
-.coordinateRow {
-    display: flex;
-    justify-content: space-between;
-    align-items: center;
-    gap: 25px;
-
-    width: min(520px, 100%);
-}
-
-.segmentedControl {
-    display: flex;
-
-    padding: 3px;
-
-    border-radius: 8px;
 
-    background-color: var(--ui-shade-3);
-}
-
-.segmentButton {
-    padding: 8px 18px;
-
-    border: 0;
-    border-radius: 6px;
-
-    background: transparent;
-    color: rgba(255, 255, 255, 0.65);
-
-    cursor: pointer;
-
-    transition:
-        background-color 0.15s ease,
-        color 0.15s ease;
-}
-
-.segmentButton:hover {
-    color: white;
-}
+    flex-wrap: wrap;
 
-.segmentButtonActive {
-    background-color: rgba(255, 255, 255, 0.13);
-    color: white;
+    gap: 10px;
 }
\ No newline at end of file
diff --git a/client/src/apps/settings/components/categories/BoardAndPieces/index.tsx b/client/src/apps/settings/components/categories/BoardAndPieces/index.tsx
index d8113ea..157769c 100644
--- a/client/src/apps/settings/components/categories/BoardAndPieces/index.tsx
+++ b/client/src/apps/settings/components/categories/BoardAndPieces/index.tsx
@@ -1,178 +1,464 @@
 import React, { useState } from "react";
 import { useTranslation } from "react-i18next";
 
-import useSettingsStore, { defaultSettings } from "@/stores/SettingsStore";
-import BoardColourPreset from "@/apps/settings/components/BoardColourPreset";
-import ColourSwatch from "@/components/settings/ColourSwatch";
-import Button from "@/components/common/Button";
-import Separator from "@/components/common/Separator";
+import useSettingsStore, {
+    defaultSettings
+} from "@/stores/SettingsStore";
 
-import * as categoryStyles from "../Category.module.css";
-import * as styles from "./BoardAndPieces.module.css";
+import BoardColourPreset from
+    "@/apps/settings/components/BoardColourPreset";
+
+import ColourSwatch from
+    "@/components/settings/ColourSwatch";
+
+import DropdownSetting from
+    "@/components/settings/DropdownSetting";
+
+import Button from
+    "@/components/common/Button";
+
+import Separator from
+    "@/components/common/Separator";
+
+import * as categoryStyles from
+    "../Category.module.css";
+
+import * as styles from
+    "./BoardAndPieces.module.css";
+
+import iconInterfaceDelete from
+    "@assets/img/interface/delete.svg";
 
-import iconInterfaceDelete from "@assets/img/interface/delete.svg";
 
 const boardColourPresets = [
-    { light: "#f0d9b5", dark: "#b58863", name: "wooden" },
-    { light: "#ebedd1", dark: "#729452", name: "tournament" },
-    { light: "#eae8d3", dark: "#4b7298", name: "blue" },
-    { light: "#d9e4e8", dark: "#83a5b6", name: "ice" },
-    { light: "#f5f1f0", dark: "#f8c8dc", name: "marshmallow" },
-    { light: "#b6b8e1", dark: "#9c9fd9", name: "pillow" }
+    {
+        light: "#f0d9b5",
+        dark: "#b58863",
+        name: "wooden"
+    },
+    {
+        light: "#ebedd1",
+        dark: "#729452",
+        name: "tournament"
+    },
+    {
+        light: "#eae8d3",
+        dark: "#4b7298",
+        name: "blue"
+    },
+    {
+        light: "#d9e4e8",
+        dark: "#83a5b6",
+        name: "ice"
+    },
+    {
+        light: "#f5f1f0",
+        dark: "#f8c8dc",
+        name: "marshmallow"
+    },
+    {
+        light: "#b6b8e1",
+        dark: "#9c9fd9",
+        name: "pillow"
+    }
 ];
 
+
 function BoardAndPieces() {
-    const { t } = useTranslation(["settings", "common"]);
 
-    const { settings, setSettings } = useSettingsStore();
+    const { t } = useTranslation([
+        "settings",
+        "common"
+    ]);
+
+
+    const {
+        settings,
+        setSettings
+    } = useSettingsStore();
+
 
     const [
         lightSquareColourSwatchOpen,
         setLightSquareColourSwatchOpen
     ] = useState(false);
 
+
     const [
         darkSquareColourSwatchOpen,
         setDarkSquareColourSwatchOpen
     ] = useState(false);
 
-    function setBoardColours(light: string, dark: string) {
+
+    /*
+     * Opciones del desplegable
+     * de coordenadas.
+     */
+    const coordinateOptions: Array<{
+        label: string;
+        value: "inside" | "outside";
+    }> = [
+        {
+            label:
+                t(
+                    "boardAndPieces"
+                    + ".coordinates"
+                    + ".inside"
+                ),
+
+            value: "inside"
+        },
+
+        {
+            label:
+                t(
+                    "boardAndPieces"
+                    + ".coordinates"
+                    + ".outside"
+                ),
+
+            value: "outside"
+        }
+    ];
+
+
+    function setBoardColours(
+        light: string,
+        dark: string
+    ) {
+
         setSettings(draft => {
-            draft.themes.board.lightSquareColour = light;
-            draft.themes.board.darkSquareColour = dark;
+
+            draft.themes.board
+                .lightSquareColour =
+                light;
+
+            draft.themes.board
+                .darkSquareColour =
+                dark;
 
             return draft;
         });
     }
 
-    return <div
-        className={categoryStyles.wrapper}
-        onClick={event => {
-            setLightSquareColourSwatchOpen(false);
-            setDarkSquareColourSwatchOpen(false);
-
-            event.stopPropagation();
-        }}
-    >
-        <b className={categoryStyles.header}>
-            {t("boardAndPieces.boardColour")}
-        </b>
-
-        <Separator className={categoryStyles.separator} />
-
-        <div className={categoryStyles.setting}>
-            <span>
-                {t("boardAndPieces.lightSquareColour")}
-            </span>
-
-            <ColourSwatch
-                colour={settings.themes.board.lightSquareColour}
-                onColourChange={colour => {
-                    setSettings(draft => {
-                        draft.themes.board.lightSquareColour = colour;
-                        return draft;
-                    });
-                }}
-                open={lightSquareColourSwatchOpen}
-                onToggle={setLightSquareColourSwatchOpen}
+
+    return (
+        <div
+            className={
+                categoryStyles.wrapper
+            }
+
+            onClick={event => {
+
+                setLightSquareColourSwatchOpen(
+                    false
+                );
+
+                setDarkSquareColourSwatchOpen(
+                    false
+                );
+
+                event.stopPropagation();
+            }}
+        >
+
+            {/*
+             * ==============================
+             * BOARD COLOUR
+             * ==============================
+             */}
+            <b
+                className={
+                    categoryStyles.header
+                }
+            >
+                {t(
+                    "boardAndPieces"
+                    + ".boardColour"
+                )}
+            </b>
+
+
+            <Separator
+                className={
+                    categoryStyles.separator
+                }
             />
-        </div>
 
-        <div className={categoryStyles.setting}>
-            <span>
-                {t("boardAndPieces.darkSquareColour")}
-            </span>
-
-            <ColourSwatch
-                colour={settings.themes.board.darkSquareColour}
-                onColourChange={colour => {
-                    setSettings(draft => {
-                        draft.themes.board.darkSquareColour = colour;
-                        return draft;
-                    });
-                }}
-                open={darkSquareColourSwatchOpen}
-                onToggle={setDarkSquareColourSwatchOpen}
+
+            <div
+                className={
+                    categoryStyles.setting
+                }
+            >
+
+                <span>
+                    {t(
+                        "boardAndPieces"
+                        + ".lightSquareColour"
+                    )}
+                </span>
+
+
+                <ColourSwatch
+                    colour={
+                        settings.themes.board
+                            .lightSquareColour
+                    }
+
+                    onColourChange={colour => {
+
+                        setSettings(draft => {
+
+                            draft.themes.board
+                                .lightSquareColour =
+                                colour;
+
+                            return draft;
+                        });
+                    }}
+
+                    open={
+                        lightSquareColourSwatchOpen
+                    }
+
+                    onToggle={
+                        setLightSquareColourSwatchOpen
+                    }
+                />
+
+            </div>
+
+
+            <div
+                className={
+                    categoryStyles.setting
+                }
+            >
+
+                <span>
+                    {t(
+                        "boardAndPieces"
+                        + ".darkSquareColour"
+                    )}
+                </span>
+
+
+                <ColourSwatch
+                    colour={
+                        settings.themes.board
+                            .darkSquareColour
+                    }
+
+                    onColourChange={colour => {
+
+                        setSettings(draft => {
+
+                            draft.themes.board
+                                .darkSquareColour =
+                                colour;
+
+                            return draft;
+                        });
+                    }}
+
+                    open={
+                        darkSquareColourSwatchOpen
+                    }
+
+                    onToggle={
+                        setDarkSquareColourSwatchOpen
+                    }
+                />
+
+            </div>
+
+
+            <Button
+                icon={
+                    iconInterfaceDelete
+                }
+
+                onClick={() => (
+                    setBoardColours(
+                        defaultSettings
+                            .themes
+                            .board
+                            .lightSquareColour,
+
+                        defaultSettings
+                            .themes
+                            .board
+                            .darkSquareColour
+                    )
+                )}
+            >
+                {t(
+                    "reset",
+                    { ns: "common" }
+                )}
+            </Button>
+
+
+            {/*
+             * ==============================
+             * PRESETS
+             * ==============================
+             */}
+            <b
+                className={
+                    categoryStyles.subheader
+                }
+            >
+                {t(
+                    "boardAndPieces"
+                    + ".presetsTitle"
+                )}
+            </b>
+
+
+            <div className={styles.presets}>
+
+                {boardColourPresets.map(
+                    preset => (
+
+                        <BoardColourPreset
+                            key={
+                                preset.name
+                            }
+
+                            lightSquareColour={
+                                preset.light
+                            }
+
+                            darkSquareColour={
+                                preset.dark
+                            }
+
+                            title={
+                                t(
+                                    `boardAndPieces.presets.${preset.name}`
+                                )
+                            }
+
+                            selected={
+                                settings
+                                    .themes
+                                    .board
+                                    .lightSquareColour
+                                    == preset.light
+
+                                &&
+
+                                settings
+                                    .themes
+                                    .board
+                                    .darkSquareColour
+                                    == preset.dark
+                            }
+
+                            onClick={() => (
+                                setBoardColours(
+                                    preset.light,
+                                    preset.dark
+                                )
+                            )}
+                        />
+
+                    )
+                )}
+
+            </div>
+
+
+            {/*
+             * ==============================
+             * COORDINATES
+             * ==============================
+             */}
+            <Separator
+                className={
+                    categoryStyles.separator
+                }
             />
-        </div>
 
-        <Button
-            icon={iconInterfaceDelete}
-            onClick={() => setBoardColours(
-                defaultSettings.themes.board.lightSquareColour,
-                defaultSettings.themes.board.darkSquareColour
-            )}
-        >
-            {t("reset", { ns: "common" })}
-        </Button>
-
-        <b className={categoryStyles.subheader}>
-            {t("boardAndPieces.presetsTitle")}
-        </b>
-
-        <div className={styles.presets}>
-            {boardColourPresets.map(preset => <BoardColourPreset
-                key={preset.name}
-                lightSquareColour={preset.light}
-                darkSquareColour={preset.dark}
-                title={t(
-                    `boardAndPieces.presets.${preset.name}`
+
+            <b
+                className={
+                    categoryStyles.subheader
+                }
+            >
+                {t(
+                    "boardAndPieces"
+                    + ".coordinates"
+                    + ".title"
                 )}
-                selected={
-                    settings.themes.board.lightSquareColour == preset.light
-                    && settings.themes.board.darkSquareColour == preset.dark
+            </b>
+
+
+            <div
+                className={
+                    categoryStyles.setting
                 }
-                onClick={() => setBoardColours(preset.light, preset.dark)}
-            />)}
+            >
+
+                <span>
+                    {t(
+                        "boardAndPieces"
+                        + ".coordinates"
+                        + ".position"
+                    )}
+                </span>
+
+
+                <DropdownSetting
+                    options={
+                        coordinateOptions
+                    }
+
+                    defaultValue={
+                        coordinateOptions.find(
+                            option =>
+                                option.value
+                                ==
+                                settings
+                                    .themes
+                                    .board
+                                    .coordinates
+                        )
+                    }
+
+                    onSelect={option => {
+
+                        if (!option) {
+                            return;
+                        }
+
+
+                        setSettings(draft => {
+
+                            draft.themes.board
+                                .coordinates =
+                                option.value;
+
+                            return draft;
+                        });
+                    }}
+
+                    dropdownStyle={{
+                        width: "200px"
+                    }}
+
+                    menuStyle={{
+                        width: "200px"
+                    }}
+                />
+
+            </div>
+
         </div>
-        <b className={categoryStyles.subheader}>
-    {t("boardAndPieces.coordinates.title")}
-</b>
-
-<Separator className={categoryStyles.separator} />
-
-<div className={styles.coordinateRow}>
-    <span>
-        {t("boardAndPieces.coordinates.position")}
-    </span>
-
-    <div className={styles.segmentedControl}>
-        <button
-            type="button"
-            className={`${styles.segmentButton} ${
-                settings.themes.board.coordinates == "inside"
-                    ? styles.segmentButtonActive
-                    : ""
-            }`}
-            onClick={() => {
-                setSettings(draft => {
-                    draft.themes.board.coordinates = "inside";
-                    return draft;
-                });
-            }}
-        >
-            {t("boardAndPieces.coordinates.inside")}
-        </button>
-
-        <button
-            type="button"
-            className={`${styles.segmentButton} ${
-                settings.themes.board.coordinates == "outside"
-                    ? styles.segmentButtonActive
-                    : ""
-            }`}
-            onClick={() => {
-                setSettings(draft => {
-                    draft.themes.board.coordinates = "outside";
-                    return draft;
-                });
-            }}
-        >
-            {t("boardAndPieces.coordinates.outside")}
-        </button>
-    </div>
-</div>
-    </div>;
+    );
 }
 
+
 export default BoardAndPieces;
\ No newline at end of file
diff --git a/client/src/components/layout/NavigationBar/NavigationBar.module.css b/client/src/components/layout/NavigationBar/NavigationBar.module.css
index 32ba7ca..b20eeec 100644
--- a/client/src/components/layout/NavigationBar/NavigationBar.module.css
+++ b/client/src/components/layout/NavigationBar/NavigationBar.module.css
@@ -1,135 +1,163 @@
 .wrapper {
     display: flex;
+
     justify-content: space-between;
     align-items: center;
+
     gap: 10px;
-    padding: 0 10px;
+
     box-sizing: border-box;
+
     width: 100%;
     height: 65px;
-    z-index: 100;
+
+    padding: 0 10px;
+
     position: sticky;
+
     top: 0;
 
-    background-color: var(--ui-shade-2);
+    z-index: 100;
+
+    background-color:
+        var(--ui-shade-2);
+
     color: white;
 
     overflow-x: auto;
 }
 
+
 .wrapper::-webkit-scrollbar {
     height: 5px;
 }
 
+
+/*
+ * Bot├│n de men├║ para
+ * pantallas peque├▒as.
+ */
 .menuButton {
     display: none;
 
     cursor: pointer;
+
     transition-duration: 0.3s;
 }
 
+
 .menuButton:hover {
     transform: scale(1.05);
 }
 
+
 @media (max-width: 800px) {
+
     .menuButton {
         display: block;
     }
+
 }
 
+
+/*
+ * Logo WintrChess.
+ */
 @media (max-width: 1000px) {
+
     .typographyText {
         display: none;
     }
+
 }
 
+
 @media (max-width: 800px) {
+
     .typographyText {
         display: flex;
     }
+
 }
 
+
 @media (max-width: 700px) {
+
     .typographyText {
         display: none;
     }
+
 }
 
+
+/*
+ * Contenedores horizontales
+ * principales.
+ */
 .section {
     display: flex;
+
     align-items: center;
+
     gap: 10px;
+
     flex-shrink: 0;
+
     height: 100%;
 }
 
+
+/*
+ * Navegaci├│n principal:
+ *
+ * Analysis
+ * Archive
+ * News
+ * Flip Board
+ * Share
+ */
 .tabs {
     display: flex;
-    flex-shrink: 0;
-    margin-left: 5px;
-    max-width: 100%;
-    height: 100%;
-}
 
-@media (max-width: 800px) {
-    .tabs {
-        display: none;
-    }
-}
-
-.support {
-    background: linear-gradient(
-        -225deg, #22e1FF 0%, #1d8fe1 48%, #625eb1 100%
-    );
-}
+    flex-shrink: 0;
 
-.signIn {
-    gap: 5px;
-    padding: 7px;
-    padding-right: 10px;
+    margin-left: 5px;
 
-    font-family: "Nunito";    
-}
+    max-width: 100%;
 
-.settings {
-    padding: 7px;
-    
-    background-color: var(--ui-shade-2);
+    height: 100%;
 }
 
-.profileMenu {
-    padding: 0 5px;
 
-    user-select: none;
-}
-
-.profileIcon {
-    display: none;
-    height: 35px;
-}
+@media (max-width: 800px) {
 
-@media (max-width: 450px) {
-    .profileUsername {
+    .tabs {
         display: none;
     }
 
-    .profileIcon {
-        display: inline;
-    }
 }
 
-.profileImage {
-    height: 42px;
-    aspect-ratio: 1 / 1;
-
-    border-radius: 50%;
-    background-color: aquamarine;
-}
 
+/*
+ * ACCIONES DE NAVEGACI├ôN
+ *
+ * Flip Board
+ * Share
+ *
+ * Esta clase replica visualmente
+ * HoverDropdown.
+ *
+ * Por eso se comportan igual que:
+ *
+ * Analysis
+ * Archive
+ * News
+ */
 .navAction {
     display: flex;
+
     align-items: center;
+
     gap: 10px;
 
     height: 100%;
@@ -139,6 +167,7 @@
     border: 0;
 
     background: transparent;
+
     color: white;
 
     font-size: 1.1rem;
@@ -148,6 +177,14 @@
     transition-duration: 0.3s;
 }
 
+
+/*
+ * L├¡nea azul inferior
+ * al pasar el cursor.
+ *
+ * Es el mismo efecto que usan
+ * Analysis / Archive / News.
+ */
 .navAction:hover {
     box-shadow:
         0 -3px 0 0
@@ -158,14 +195,74 @@
         rgba(0, 0, 0, 0.15);
 }
 
+
+/*
+ * Tama├▒o de los iconos
+ * de Flip Board y Share.
+ */
 .navAction img {
     width: 25px;
     height: 25px;
+
+    object-fit: contain;
 }
 
-.topAction {
+
+/*
+ * SIGN IN.
+ */
+.signIn {
+    gap: 5px;
+
+    padding: 7px;
+
+    padding-right: 10px;
+}
+
+
+/*
+ * SETTINGS.
+ *
+ * Se queda arriba a la derecha.
+ *
+ * No tiene la animaci├│n azul
+ * porque no forma parte
+ * de la navegaci├│n principal.
+ */
+.settings {
     padding: 7px;
 
     background-color:
         var(--ui-shade-2);
+}
+
+
+/*
+ * Men├║ del usuario autenticado.
+ */
+.profileMenu {
+    padding: 0 5px;
+
+    user-select: none;
+}
+
+
+.profileIcon {
+    display: none;
+
+    height: 35px;
+}
+
+
+@media (max-width: 450px) {
+
+    .profileUsername {
+        display: none;
+    }
+
+
+    .profileIcon {
+        display: inline;
+    }
+
 }
\ No newline at end of file
diff --git a/client/src/components/layout/NavigationBar/index.tsx b/client/src/components/layout/NavigationBar/index.tsx
index bcbd869..c270977 100644
--- a/client/src/components/layout/NavigationBar/index.tsx
+++ b/client/src/components/layout/NavigationBar/index.tsx
@@ -14,7 +14,9 @@ import ShareDialog from
     "@analysis/components/ShareDialog";
 
 import Typography from "@/components/Typography";
+
 import Button from "@/components/common/Button";
+
 import BlurBackground from
     "@/components/layout/BlurBackground";
 
@@ -40,9 +42,6 @@ import iconIconsArchive from
 import iconIconsNews from
     "@assets/img/icons/news.png";
 
-import iconKofi from
-    "@assets/img/kofi.svg";
-
 import iconInterfaceSignin from
     "@assets/img/interface/sign_in.svg";
 
@@ -60,42 +59,101 @@ import iconShare from
 
 
 function NavigationBar() {
+
+    /*
+     * Usamos traducciones de:
+     *
+     * common
+     * analysis
+     */
     const { t } = useTranslation([
         "common",
         "analysis"
     ]);
 
+
+    /*
+     * Informaci├│n del usuario autenticado.
+     */
     const {
         profile,
         status
     } = useAuthedProfile();
 
-    const [sidebarOpen, setSidebarOpen] =
-        useState(false);
 
-    const [shareOpen, setShareOpen] =
-        useState(false);
+    /*
+     * Control del men├║ lateral para
+     * pantallas peque├▒as.
+     */
+    const [
+        sidebarOpen,
+        setSidebarOpen
+    ] = useState(false);
+
 
+    /*
+     * Control del di├ílogo de compartir.
+     */
+    const [
+        shareOpen,
+        setShareOpen
+    ] = useState(false);
 
+
+    /*
+     * Solo mostramos Flip Board y Share
+     * cuando estamos en la p├ígina Analysis.
+     */
     const onAnalysisPage =
         location.pathname.startsWith(
             "/analysis"
         );
 
 
-    const analysisGame =
-        useAnalysisGameStore(
-            state => state.analysisGame
+    /*
+     * Partida actualmente cargada.
+     *
+     * ShareDialog necesita esta informaci├│n.
+     */
+    const analysisGame = useAnalysisGameStore(
+        state => state.analysisGame
+    );
+
+
+    /*
+     * Nodo actual del ├írbol de posiciones.
+     *
+     * ShareDialog tambi├®n lo necesita.
+     */
+    const currentStateTreeNode =
+        useAnalysisBoardStore(
+            state =>
+                state.currentStateTreeNode
         );
 
 
-    const {
-        currentStateTreeNode,
-        boardFlipped,
-        setBoardFlipped
-    } = useAnalysisBoardStore();
+    /*
+     * Estado de orientaci├│n del tablero.
+     */
+    const boardFlipped =
+        useAnalysisBoardStore(
+            state => state.boardFlipped
+        );
 
 
+    /*
+     * Funci├│n para cambiar
+     * la orientaci├│n del tablero.
+     */
+    const setBoardFlipped =
+        useAnalysisBoardStore(
+            state => state.setBoardFlipped
+        );
+
+
+    /*
+     * Cerrar sesi├│n.
+     */
     async function signOut() {
         await authClient.signOut();
 
@@ -106,31 +164,61 @@ function NavigationBar() {
     return (
         <div className={styles.wrapper}>
 
+            {/*
+             * =====================================================
+             * ZONA IZQUIERDA
+             * =====================================================
+             *
+             * Logo
+             * Analysis
+             * Archive
+             * News
+             * Flip Board
+             * Share
+             */}
             <div className={styles.section}>
 
+                {/*
+                 * Logo + bot├│n men├║ m├│vil
+                 */}
                 <div className={styles.section}>
+
                     <img
-                        className={styles.menuButton}
-                        src={iconInterfaceMenu}
+                        className={
+                            styles.menuButton
+                        }
+                        src={
+                            iconInterfaceMenu
+                        }
                         height={35}
                         onClick={() => (
                             setSidebarOpen(true)
                         )}
                     />
 
+
                     <Typography
                         textClassName={
                             styles.typographyText
                         }
                         includeIcon
                     />
+
                 </div>
 
 
+                {/*
+                 * Navegaci├│n principal
+                 */}
                 <div className={styles.tabs}>
 
+                    {/*
+                     * ANALYSIS
+                     */}
                     <HoverDropdown
-                        icon={iconIconsAnalysis}
+                        icon={
+                            iconIconsAnalysis
+                        }
                         url="/analysis"
                     >
                         {t(
@@ -140,8 +228,13 @@ function NavigationBar() {
                     </HoverDropdown>
 
 
+                    {/*
+                     * ARCHIVE
+                     */}
                     <HoverDropdown
-                        icon={iconIconsArchive}
+                        icon={
+                            iconIconsArchive
+                        }
                         url="/archive"
                     >
                         {t(
@@ -151,8 +244,13 @@ function NavigationBar() {
                     </HoverDropdown>
 
 
+                    {/*
+                     * NEWS
+                     */}
                     <HoverDropdown
-                        icon={iconIconsNews}
+                        icon={
+                            iconIconsNews
+                        }
                         url="/news"
                     >
                         {t(
@@ -162,6 +260,16 @@ function NavigationBar() {
                     </HoverDropdown>
 
 
+                    {/*
+                     * FLIP BOARD
+                     *
+                     * Solo en /analysis.
+                     *
+                     * Usa nuestra clase navAction
+                     * para tener la misma animaci├│n
+                     * inferior azul que Analysis,
+                     * Archive y News.
+                     */}
                     {onAnalysisPage && (
                         <button
                             type="button"
@@ -181,72 +289,77 @@ function NavigationBar() {
 
                             <span>
                                 {t(
-                                    "optionsToolbar.flipBoard",
-                                    { ns: "analysis" }
+                                    "optionsToolbar"
+                                    + ".flipBoard",
+                                    {
+                                        ns:
+                                            "analysis"
+                                    }
                                 )}
                             </span>
                         </button>
                     )}
 
-                </div>
-
-            </div>
-
-
-            <div className={styles.section}>
-
-                <a
-                    href="https://ko-fi.com/wintrcat"
-                    target="_blank"
-                >
-                    <Button
-                        className={styles.support}
-                        icon={iconKofi}
-                        tooltipId={
-                            "navigation-bar-support"
-                        }
-                    />
-                </a>
-
 
-                <Tooltip
-                    id="navigation-bar-support"
-                    content={t(
-                        "navigationBar.tooltips.support",
-                        { ns: "common" }
-                    )}
-                    delayShow={500}
-                />
-
-
-                {onAnalysisPage && (
-                    <>
-                        <Button
+                    {/*
+                     * SHARE
+                     *
+                     * Ahora vive al lado
+                     * de Flip Board.
+                     *
+                     * Ya NO estar├í como
+                     * icono suelto a la derecha.
+                     */}
+                    {onAnalysisPage && (
+                        <button
+                            type="button"
                             className={
-                                styles.topAction
-                            }
-                            icon={iconShare}
-                            iconSize="28px"
-                            tooltipId={
-                                "navigation-bar-share"
+                                styles.navAction
                             }
                             onClick={() => (
                                 setShareOpen(true)
                             )}
-                        />
+                        >
+                            <img
+                                src={iconShare}
+                                alt=""
+                            />
+
+                            <span>
+                                {t(
+                                    "optionsToolbar"
+                                    + ".share",
+                                    {
+                                        ns:
+                                            "analysis"
+                                    }
+                                )}
+                            </span>
+                        </button>
+                    )}
+
+                </div>
+
+            </div>
 
-                        <Tooltip
-                            id="navigation-bar-share"
-                            content={t(
-                                "optionsToolbar.share",
-                                { ns: "analysis" }
-                            )}
-                            delayShow={500}
-                        />
-                    </>
-                )}
 
+            {/*
+             * =====================================================
+             * ZONA DERECHA
+             * =====================================================
+             *
+             * Usuario / Sign In
+             * Settings
+             *
+             * NO Ko-fi.
+             * NO Share.
+             */}
+            <div className={styles.section}>
 
+                {/*
+                 * Mientras se comprueba
+                 * la sesi├│n del usuario.
+                 */}
                 {status == "pending" && (
                     <span>
                         {t(
@@ -257,6 +370,9 @@ function NavigationBar() {
                 )}
 
 
+                {/*
+                 * USUARIO CON SESI├ôN INICIADA
+                 */}
                 {status == "success" && (
                     <HoverDropdown
                         dropdownClassName={
@@ -274,7 +390,10 @@ function NavigationBar() {
                                         "navigationBar"
                                         + ".profileMenu"
                                         + ".signOut",
-                                        { ns: "common" }
+                                        {
+                                            ns:
+                                                "common"
+                                        }
                                     ),
 
                                 onClick:
@@ -282,6 +401,7 @@ function NavigationBar() {
                             }
                         ]}
                     >
+
                         <span
                             className={
                                 styles.profileUsername
@@ -290,6 +410,7 @@ function NavigationBar() {
                             {profile.username}
                         </span>
 
+
                         <img
                             className={
                                 styles.profileIcon
@@ -298,12 +419,17 @@ function NavigationBar() {
                                 iconInterfaceAccount
                             }
                         />
+
                     </HoverDropdown>
                 )}
 
 
+                {/*
+                 * USUARIO NO AUTENTICADO
+                 */}
                 {status == "error" && (
                     <a href="/signin">
+
                         <Button
                             className={
                                 styles.signIn
@@ -314,22 +440,33 @@ function NavigationBar() {
                             iconSize="28px"
                         >
                             {t(
-                                "navigationBar.signIn",
-                                { ns: "common" }
+                                "navigationBar"
+                                + ".signIn",
+                                {
+                                    ns:
+                                        "common"
+                                }
                             )}
                         </Button>
+
                     </a>
                 )}
 
 
                 {/*
-                 * ├ÜNICO ACCESO A SETTINGS.
-                 * Siempre arriba a la derecha.
+                 * SETTINGS
+                 *
+                 * Este es el ├ÜNICO acceso
+                 * principal a Settings.
+                 *
+                 * Siempre est├í visible
+                 * arriba a la derecha.
                  */}
                 <a href="/settings">
+
                     <Button
                         className={
-                            styles.topAction
+                            styles.settings
                         }
                         icon={
                             iconIconsSettings
@@ -339,11 +476,14 @@ function NavigationBar() {
                             "navigation-bar-settings"
                         }
                     />
+
                 </a>
 
 
                 <Tooltip
-                    id="navigation-bar-settings"
+                    id={
+                        "navigation-bar-settings"
+                    }
                     content={t(
                         "settings",
                         { ns: "common" }
@@ -354,9 +494,15 @@ function NavigationBar() {
             </div>
 
 
+            {/*
+             * Fondo desenfocado
+             * cuando se abre el Sidebar.
+             */}
             {sidebarOpen && (
                 <BlurBackground
-                    style={{ zIndex: 1000 }}
+                    style={{
+                        zIndex: 1000
+                    }}
                     onClick={() => (
                         setSidebarOpen(false)
                     )}
@@ -364,9 +510,13 @@ function NavigationBar() {
             )}
 
 
+            {/*
+             * Sidebar m├│vil.
+             */}
             <Sidebar
                 style={{
                     zIndex: 1001,
+
                     transition:
                         "left 0.3s ease",
 
@@ -381,9 +531,20 @@ function NavigationBar() {
             />
 
 
+            {/*
+             * SHARE DIALOG.
+             *
+             * Sigue existiendo exactamente
+             * igual que antes.
+             *
+             * Lo ├║nico que hemos cambiado
+             * es el bot├│n que lo abre.
+             */}
             {shareOpen && (
                 <ShareDialog
-                    game={analysisGame}
+                    game={
+                        analysisGame
+                    }
                     currentNode={
                         currentStateTreeNode
                     }
diff --git a/server/src/routes/api/analysis/analyse.ts b/server/src/routes/api/analysis/analyse.ts
index 4a4d1d9..baa619c 100644
--- a/server/src/routes/api/analysis/analyse.ts
+++ b/server/src/routes/api/analysis/analyse.ts
@@ -19,6 +19,43 @@ router.use(path,
     express.json({ limit: "1mb" })
 );
 
+function parseOptionalNumber(
+    value: unknown
+) {
+
+    if (
+        typeof value
+        != "string"
+
+        ||
+
+        value.trim()
+        == ""
+    ) {
+
+        return undefined;
+    }
+
+
+    const parsed =
+        Number(
+            value
+        );
+
+
+    if (
+        !Number.isFinite(
+            parsed
+        )
+    ) {
+
+        return undefined;
+    }
+
+
+    return parsed;
+}
+
 router.post(path, async (req, res) => {
     const serializedStateTree: SerializedStateTreeNode = req.body;
 
@@ -28,11 +65,50 @@ router.post(path, async (req, res) => {
     const stateTree = deserializeNode(serializedStateTree);
 
     try {
-        const gameAnalysis = getGameAnalysis(stateTree, {
-            includeBrilliant: req.query.brilliant == "true",
-            includeCritical: req.query.critical == "true",
-            includeTheory: req.query.theory == "true"
-        });
+        const gameAnalysis = getGameAnalysis(
+    stateTree,
+    {
+        includeBrilliant:
+            req
+                .query
+                .brilliant
+            == "true",
+
+        includeCritical:
+            req
+                .query
+                .critical
+            == "true",
+
+        includeTheory:
+            req
+                .query
+                .theory
+            == "true",
+
+
+        whiteRating:
+
+            parseOptionalNumber(
+
+                req
+                    .query
+                    .whiteRating
+
+            ),
+
+
+        blackRating:
+
+            parseOptionalNumber(
+
+                req
+                    .query
+                    .blackRating
+
+            )
+    }
+);
 
         res.json({
             ...gameAnalysis,
diff --git a/shared/src/constants/Classification.ts b/shared/src/constants/Classification.ts
index fcefe0e..8605324 100644
--- a/shared/src/constants/Classification.ts
+++ b/shared/src/constants/Classification.ts
@@ -1,37 +1,126 @@
 export enum Classification {
     BRILLIANT = "brilliant",
     CRITICAL = "critical",
+
     BEST = "best",
     EXCELLENT = "excellent",
     OKAY = "okay",
+
     INACCURACY = "inaccuracy",
     MISTAKE = "mistake",
+
+    /*
+     * Miss real.
+     *
+     * Representa una oportunidad
+     * importante creada por un error
+     * del rival que no fue aprovechada.
+     */
+    MISS = "miss",
+
     BLUNDER = "blunder",
+
     THEORY = "theory",
     FORCED = "forced",
+
+    /*
+     * Conservamos RISKY
+     * por compatibilidad con
+     * c├│digo antiguo de WintrChess.
+     *
+     * M├ís adelante decidiremos
+     * si lo eliminamos completamente.
+     */
     RISKY = "risky"
 }
 
-export const classifValues: Record<Classification, number> = {
-    [Classification.BLUNDER]: 0,
-    [Classification.MISTAKE]: 1,
-    [Classification.INACCURACY]: 2,
-    [Classification.RISKY]: 2,
-    [Classification.OKAY]: 3,
-    [Classification.EXCELLENT]: 4,
-    [Classification.BEST]: 5,
-    [Classification.CRITICAL]: 5,
-    [Classification.BRILLIANT]: 5,
-    [Classification.FORCED]: 5,
-    [Classification.THEORY]: 5
+
+/*
+ * Valor relativo de calidad.
+ *
+ * Se utiliza internamente
+ * para comparar categor├¡as.
+ */
+export const classifValues:
+    Record<
+        Classification,
+        number
+    > = {
+
+    [Classification.BLUNDER]:
+        0,
+
+    /*
+     * Un Miss no es necesariamente
+     * tan destructivo como un Blunder,
+     * pero tampoco es una jugada buena.
+     */
+    [Classification.MISS]:
+        1,
+
+    [Classification.MISTAKE]:
+        1,
+
+    [Classification.INACCURACY]:
+        2,
+
+    [Classification.RISKY]:
+        2,
+
+    [Classification.OKAY]:
+        3,
+
+    [Classification.EXCELLENT]:
+        4,
+
+    [Classification.BEST]:
+        5,
+
+    [Classification.CRITICAL]:
+        5,
+
+    [Classification.BRILLIANT]:
+        5,
+
+    [Classification.FORCED]:
+        5,
+
+    [Classification.THEORY]:
+        5
 };
 
-// https://en.wikipedia.org/wiki/Portable_Game_Notation#Standard_NAGs
-export const classifNags: Record<string, string | undefined> = {
-    [Classification.BRILLIANT]: "$3",
-    [Classification.CRITICAL]: "$1",
-    [Classification.INACCURACY]: "$6",
-    [Classification.MISTAKE]: "$2",
-    [Classification.BLUNDER]: "$4",
-    [Classification.RISKY]: "$5"
+
+/*
+ * Standard PGN NAG values.
+ *
+ * Miss no tiene un NAG est├índar
+ * perfectamente equivalente,
+ * as├¡ que no inventamos uno.
+ */
+export const classifNags:
+    Record<
+        string,
+        string | undefined
+    > = {
+
+    [Classification.BRILLIANT]:
+        "$3",
+
+    [Classification.CRITICAL]:
+        "$1",
+
+    [Classification.INACCURACY]:
+        "$6",
+
+    [Classification.MISTAKE]:
+        "$2",
+
+    [Classification.BLUNDER]:
+        "$4",
+
+    [Classification.RISKY]:
+        "$5",
+
+    [Classification.MISS]:
+        undefined
 };
\ No newline at end of file
diff --git a/shared/src/lib/reporter/classification/pointLoss.ts b/shared/src/lib/reporter/classification/pointLoss.ts
index 23f2c33..54aa1a9 100644
--- a/shared/src/lib/reporter/classification/pointLoss.ts
+++ b/shared/src/lib/reporter/classification/pointLoss.ts
@@ -1,109 +1,447 @@
-import { WHITE } from "chess.js";
+import {
+    WHITE
+} from "chess.js";
 
 import {
     ExtractedCurrentNode,
     ExtractedPreviousNode
 } from "../types/ExtractedNode";
-import { Classification } from "@/constants/Classification";
-import { adaptPieceColour } from "@/constants/PieceColour";
-import { getExpectedPointsLoss } from "../expectedPoints";
+
+import {
+    Classification
+} from "@/constants/Classification";
+
+import {
+    adaptPieceColour
+} from "@/constants/PieceColour";
+
+import {
+    getExpectedPointsLoss
+} from "../expectedPoints";
+
+
+/*
+ * ============================================================
+ * CLASSIFICATION V2
+ * BASED ON EXPECTED POINTS LOSS
+ * ============================================================
+ *
+ *
+ * BEST
+ *
+ * top move
+ * OR
+ * EP loss <= 0.001
+ *
+ *
+ * EXCELLENT
+ *
+ * 0.001 < EP loss <= 0.02
+ *
+ *
+ * GOOD
+ *
+ * 0.02 < EP loss <= 0.05
+ *
+ *
+ * INACCURACY
+ *
+ * 0.05 < EP loss <= 0.10
+ *
+ *
+ * MISTAKE
+ *
+ * 0.10 < EP loss <= 0.20
+ *
+ *
+ * BLUNDER
+ *
+ * EP loss > 0.20
+ */
+
 
 /**
- * @description Classify using two evaluations and a move colour,
- * using expected point losses or mate losses.
+ * Classify a move using
+ * Expected Points Loss.
+ *
+ * Mate transitions keep their
+ * specialised WintrChess treatment
+ * because Expected Points alone
+ * collapses mate evaluations
+ * too aggressively.
  */
 export function pointLossClassify(
-    previous: ExtractedPreviousNode,
-    current: ExtractedCurrentNode
+    previous:
+        ExtractedPreviousNode,
+
+    current:
+        ExtractedCurrentNode
 ) {
-    const previousSubjectiveValue = previous.evaluation.value * (
-        (current.playedMove.color == WHITE ? 1 : -1)
-    );
 
-    const subjectiveValue = current.subjectiveEvaluation.value;
+    const previousSubjectiveValue =
+
+        previous
+            .evaluation
+            .value
+
+        *
+
+        (
+            current
+                .playedMove
+                .color
+            == WHITE
+
+                ? 1
+                : -1
+        );
+
+
+    const subjectiveValue =
+
+        current
+            .subjectiveEvaluation
+            .value;
+
+
+    /*
+     * ========================================================
+     * MATE -> MATE
+     * ========================================================
+     */
 
-    // Mate to mate evaluations
     if (
-        previous.evaluation.type == "mate"
-        && current.evaluation.type == "mate"
+
+        previous
+            .evaluation
+            .type
+        == "mate"
+
+        &&
+
+        current
+            .evaluation
+            .type
+        == "mate"
+
     ) {
-        // Winning mate to losing mate
-        if (previousSubjectiveValue > 0 && subjectiveValue < 0) {
-            return subjectiveValue < -3
-                ? Classification.MISTAKE
-                : Classification.BLUNDER;
+
+        /*
+         * Winning mate
+         * becomes losing mate.
+         */
+        if (
+
+            previousSubjectiveValue
+            > 0
+
+            &&
+
+            subjectiveValue
+            < 0
+
+        ) {
+
+            return (
+
+                subjectiveValue
+                < -3
+
+                    ? Classification.MISTAKE
+
+                    : Classification.BLUNDER
+
+            );
         }
 
-        // For the losing side, making a move that keeps the mate the same
-        // is best. Only the winning side expects a mate loss of -1.
-        const mateLoss = (
-            (current.evaluation.value - previous.evaluation.value)
-            * (current.playedMove.color == WHITE ? 1 : -1)
-        );
 
-        if (mateLoss < 0 || (mateLoss == 0 && subjectiveValue < 0)) {
+        /*
+         * Difference in mate distance.
+         */
+        const mateLoss =
+
+            (
+
+                current
+                    .evaluation
+                    .value
+
+                -
+
+                previous
+                    .evaluation
+                    .value
+
+            )
+
+            *
+
+            (
+                current
+                    .playedMove
+                    .color
+                == WHITE
+
+                    ? 1
+                    : -1
+            );
+
+
+        /*
+         * Losing side maintaining
+         * the same forced mate
+         * is effectively Best.
+         */
+        if (
+
+            mateLoss < 0
+
+            ||
+
+            (
+
+                mateLoss == 0
+
+                &&
+
+                subjectiveValue < 0
+
+            )
+
+        ) {
+
             return Classification.BEST;
-        } else if (mateLoss < 2) {
+        }
+
+
+        if (
+            mateLoss < 2
+        ) {
+
             return Classification.EXCELLENT;
-        } else if (mateLoss < 7) {
+        }
+
+
+        if (
+            mateLoss < 7
+        ) {
+
             return Classification.OKAY;
-        } else {
-            return Classification.INACCURACY;
         }
+
+
+        return Classification.INACCURACY;
     }
 
-    // Mate to centipawn evaluations
+
+    /*
+     * ========================================================
+     * MATE -> CENTIPAWN
+     * ========================================================
+     */
+
     if (
-        previous.evaluation.type == "mate"
-        && current.evaluation.type == "centipawn"
+
+        previous
+            .evaluation
+            .type
+        == "mate"
+
+        &&
+
+        current
+            .evaluation
+            .type
+        == "centipawn"
+
     ) {
-        if (subjectiveValue >= 800) {
+
+        if (
+            subjectiveValue >= 800
+        ) {
+
             return Classification.EXCELLENT;
-        } else if (subjectiveValue >= 400) {
+        }
+
+
+        if (
+            subjectiveValue >= 400
+        ) {
+
             return Classification.OKAY;
-        } else if (subjectiveValue >= 200) {
+        }
+
+
+        if (
+            subjectiveValue >= 200
+        ) {
+
             return Classification.INACCURACY;
-        } else if (subjectiveValue >= 0) {
+        }
+
+
+        if (
+            subjectiveValue >= 0
+        ) {
+
             return Classification.MISTAKE;
-        } else {
-            return Classification.BLUNDER;
         }
+
+
+        return Classification.BLUNDER;
     }
 
-    // Centipawn to mate evaluations
+
+    /*
+     * ========================================================
+     * CENTIPAWN -> MATE
+     * ========================================================
+     */
+
     if (
-        previous.evaluation.type == "centipawn"
-        && current.evaluation.type == "mate"
+
+        previous
+            .evaluation
+            .type
+        == "centipawn"
+
+        &&
+
+        current
+            .evaluation
+            .type
+        == "mate"
+
     ) {
-        if (subjectiveValue > 0) {
+
+        /*
+         * Encontrar un mate ganador.
+         */
+        if (
+            subjectiveValue > 0
+        ) {
+
             return Classification.BEST;
-        } else if (subjectiveValue >= -2) {
+        }
+
+
+        /*
+         * Permitir mate inmediato.
+         */
+        if (
+            subjectiveValue >= -2
+        ) {
+
             return Classification.BLUNDER;
-        } else if (subjectiveValue >= -5) {
+        }
+
+
+        if (
+            subjectiveValue >= -5
+        ) {
+
             return Classification.MISTAKE;
-        } else {
-            return Classification.INACCURACY;
         }
+
+
+        return Classification.INACCURACY;
     }
 
-    // Centipawn to centipawn evaluations
-    const pointLoss = getExpectedPointsLoss(
-        previous.evaluation,
-        current.evaluation,
-        adaptPieceColour(current.playedMove.color)
-    );
 
-    if (pointLoss < 0.01) {
+    /*
+     * ========================================================
+     * CENTIPAWN -> CENTIPAWN
+     * ========================================================
+     *
+     * Classification V2.
+     */
+
+    const pointLoss =
+
+        getExpectedPointsLoss(
+
+            previous
+                .evaluation,
+
+            current
+                .evaluation,
+
+            adaptPieceColour(
+                current
+                    .playedMove
+                    .color
+            )
+
+        );
+
+
+    /*
+     * BEST.
+     *
+     * Esto cubre el caso se├▒alado
+     * por Gemini:
+     *
+     * segunda l├¡nea pr├ícticamente
+     * equivalente a la primera.
+     *
+     * topMovePlayed tambi├®n se
+     * comprueba posteriormente
+     * en classify.ts.
+     */
+    if (
+        pointLoss <= 0.001
+    ) {
+
         return Classification.BEST;
-    } else if (pointLoss < 0.045) {
+    }
+
+
+    /*
+     * EXCELLENT.
+     */
+    if (
+        pointLoss <= 0.02
+    ) {
+
         return Classification.EXCELLENT;
-    } else if (pointLoss < 0.08) {
+    }
+
+
+    /*
+     * GOOD.
+     */
+    if (
+        pointLoss <= 0.05
+    ) {
+
         return Classification.OKAY;
-    } else if (pointLoss < 0.12) {
+    }
+
+
+    /*
+     * INACCURACY.
+     */
+    if (
+        pointLoss <= 0.10
+    ) {
+
         return Classification.INACCURACY;
-    } else if (pointLoss < 0.22) {
+    }
+
+
+    /*
+     * MISTAKE.
+     */
+    if (
+        pointLoss <= 0.20
+    ) {
+
         return Classification.MISTAKE;
-    } else {
-        return Classification.BLUNDER;
     }
+
+
+    /*
+     * BLUNDER.
+     */
+    return Classification.BLUNDER;
 }
\ No newline at end of file
diff --git a/shared/src/lib/reporter/classify.ts b/shared/src/lib/reporter/classify.ts
index ea06851..e89f718 100644
--- a/shared/src/lib/reporter/classify.ts
+++ b/shared/src/lib/reporter/classify.ts
@@ -1,74 +1,490 @@
-import ReportOptions from "./types/AnalysisOptions";
-import { StateTreeNode } from "@/types/game/position/StateTreeNode";
-import { Classification, classifValues } from "@/constants/Classification";
+import {
+    WHITE
+} from "chess.js";
+
+import ReportOptions from
+    "./types/AnalysisOptions";
+
+import {
+    StateTreeNode
+} from "@/types/game/position/StateTreeNode";
+
+import {
+    Classification
+} from "@/constants/Classification";
+
 import {
     extractPreviousStateTreeNode,
     extractCurrentStateTreeNode
 } from "./utils/extractNode";
 
-import { getOpeningName } from "./utils/opening";
-import { pointLossClassify } from "./classification/pointLoss";
-import { considerBrilliantClassification } from "./classification/brilliant";
-import { considerCriticalClassification } from "./classification/critical";
+import {
+    getOpeningName
+} from "./utils/opening";
+
+import {
+    pointLossClassify
+} from "./classification/pointLoss";
+
+import {
+    considerMissClassification
+} from "./classification/miss";
+
+import {
+    considerBrilliantClassification
+} from "./classification/brilliant";
+
+import {
+    considerCriticalClassification
+} from "./classification/critical";
+
+
+/*
+ * ============================================================
+ * THEORY DETECTION V2
+ * ============================================================
+ */
+
+
+function isStillInOpeningTheory(
+    node:
+        StateTreeNode
+) {
+
+    /*
+     * La posici├│n actual
+     * debe pertenecer
+     * al libro.
+     */
+    const currentOpening =
+
+        getOpeningName(
+            node.state.fen
+        );
+
+
+    if (
+        !currentOpening
+    ) {
+
+        return false;
+    }
+
+
+    /*
+     * Buscamos cualquier posici├│n
+     * anterior fuera del libro.
+     *
+     * Una vez existe
+     * la primera desviaci├│n:
+     *
+     * nunca m├ís volvemos
+     * a clasificar Book.
+     */
+
+    let ancestor =
+
+        node.parent;
+
+
+    while (
+        ancestor
+    ) {
+
+        if (
+            ancestor
+                .state
+                .move
+        ) {
+
+            const ancestorOpening =
+
+                ancestor
+                    .state
+                    .opening
+
+                ??
+
+                getOpeningName(
+
+                    ancestor
+                        .state
+                        .fen
+
+                );
+
+
+            if (
+                !ancestorOpening
+            ) {
+
+                return false;
+            }
+
+        }
+
+
+        ancestor =
+            ancestor.parent;
+
+    }
+
+
+    return true;
+}
+
+
+/*
+ * ============================================================
+ * MAIN CLASSIFIER V2
+ * ============================================================
+ */
+
 
 export function classify(
-    node: StateTreeNode,
-    options?: ReportOptions
+    node:
+        StateTreeNode,
+
+    options?:
+        ReportOptions
 ) {
-    if (!node.parent) {
-        throw new Error("no parent node exists to compare with.");
+
+    if (
+        !node.parent
+    ) {
+
+        throw new Error(
+            "no parent node exists to compare with."
+        );
     }
 
-    const previous = extractPreviousStateTreeNode(node.parent);
-    const current = extractCurrentStateTreeNode(node);
 
-    if (!previous || !current) {
-        throw new Error("information missing from current or previous node.");
+    const previous =
+
+        extractPreviousStateTreeNode(
+            node.parent
+        );
+
+
+    const current =
+
+        extractCurrentStateTreeNode(
+            node
+        );
+
+
+    if (
+
+        !previous
+
+        ||
+
+        !current
+
+    ) {
+
+        throw new Error(
+            "information missing from current or previous node."
+        );
     }
 
-    const opts: Required<ReportOptions> = {
-        includeBrilliant: true,
-        includeCritical: true,
-        includeTheory: true,
-        ...options
+
+    const opts = {
+
+        includeBrilliant:
+
+            options
+                ?.includeBrilliant
+
+            ?? true,
+
+
+        includeCritical:
+
+            options
+                ?.includeCritical
+
+            ?? true,
+
+
+        includeTheory:
+
+            options
+                ?.includeTheory
+
+            ?? true,
+
+
+        whiteRating:
+
+            options
+                ?.whiteRating,
+
+
+        blackRating:
+
+            options
+                ?.blackRating
+
     };
 
-    // Consider forced classification
-    if (previous.board.moves().length <= 1) {
+
+    /*
+     * Rating del jugador
+     * que acaba de realizar
+     * la jugada actual.
+     */
+
+    const playerRating =
+
+        current
+            .playedMove
+            .color
+
+        == WHITE
+
+            ? opts
+                .whiteRating
+
+            : opts
+                .blackRating;
+
+
+    /*
+     * ========================================================
+     * 1. FORCED
+     * ========================================================
+     */
+
+    if (
+
+        previous
+            .board
+            .moves()
+            .length
+
+        <= 1
+
+    ) {
+
         return Classification.FORCED;
     }
 
-    // Consider theory classification
-    const openingName = getOpeningName(current.state.fen);
 
-    if (opts.includeTheory && openingName) {
+    /*
+     * ========================================================
+     * 2. THEORY
+     * ========================================================
+     */
+
+    if (
+
+        opts
+            .includeTheory
+
+        &&
+
+        isStillInOpeningTheory(
+            node
+        )
+
+    ) {
+
         return Classification.THEORY;
     }
 
-    // Short-circuit checkmates with best
-    if (current.board.isCheckmate()) {
+
+    /*
+     * ========================================================
+     * 3. CHECKMATE
+     * ========================================================
+     */
+
+    if (
+
+        current
+            .board
+            .isCheckmate()
+
+    ) {
+
         return Classification.BEST;
     }
 
-    const topMovePlayed = previous.topMove.san == current.playedMove.san;
 
-    // Point loss classify
-    let classification = topMovePlayed
-        ? Classification.BEST
-        : pointLossClassify(previous, current);
+    /*
+     * ========================================================
+     * 4. BASE CLASSIFICATION
+     * ========================================================
+     */
+
+
+    const topMovePlayed =
+
+        previous
+            .topMove
+            .san
+
+        ==
+
+        current
+            .playedMove
+            .san;
+
+
+    /*
+     * Best:
+     *
+     * - top engine move;
+     *
+     * O
+     *
+     * - pr├ícticamente id├®ntica
+     *   en Expected Points.
+     *
+     *
+     * El segundo caso
+     * lo controla pointLossClassify().
+     */
+
+    let classification =
+
+        topMovePlayed
+
+            ? Classification.BEST
+
+            : pointLossClassify(
+                previous,
+                current
+            );
+
+
+    /*
+     * ========================================================
+     * 5. MISS
+     * ========================================================
+     *
+     * Miss se calcula
+     * con contexto A ÔåÆ B ÔåÆ C.
+     *
+     * El propio detector evita
+     * convertir en Miss
+     * una cat├ístrofe autogenerada.
+     */
+
+    if (
+
+        considerMissClassification(
+
+            node,
+
+            playerRating
+
+        )
+
+    ) {
+
+        classification =
+            Classification.MISS;
+
+    }
+
+
+    /*
+     * ========================================================
+     * 6. GREAT
+     * ========================================================
+     */
 
-    // Consider only and brilliant classification
     if (
-        opts.includeCritical
-        && topMovePlayed
-        && considerCriticalClassification(previous, current)
-    ) classification = Classification.CRITICAL;
+
+        classification
+        != Classification.MISS
+
+        &&
+
+        opts
+            .includeCritical
+
+        &&
+
+        (
+
+            classification
+            == Classification.BEST
+
+            ||
+
+            classification
+            == Classification.EXCELLENT
+
+        )
+
+        &&
+
+        considerCriticalClassification(
+            previous,
+            current
+        )
+
+    ) {
+
+        classification =
+            Classification.CRITICAL;
+
+    }
+
+
+    /*
+     * ========================================================
+     * 7. BRILLIANT
+     * ========================================================
+     */
+
+    const brilliantEligible =
+
+        classification
+        == Classification.BEST
+
+        ||
+
+        classification
+        == Classification.EXCELLENT
+
+        ||
+
+        classification
+        == Classification.CRITICAL;
+
 
     if (
-        opts.includeBrilliant
-        && classifValues[classification] >= classifValues[Classification.BEST]
-        && considerBrilliantClassification(previous, current)
-    ) classification = Classification.BRILLIANT;
+
+        classification
+        != Classification.MISS
+
+        &&
+
+        opts
+            .includeBrilliant
+
+        &&
+
+        brilliantEligible
+
+        &&
+
+        considerBrilliantClassification(
+            previous,
+            current
+        )
+
+    ) {
+
+        classification =
+            Classification.BRILLIANT;
+
+    }
+
 
     return classification;
 }
\ No newline at end of file
diff --git a/shared/src/lib/reporter/types/AnalysisOptions.ts b/shared/src/lib/reporter/types/AnalysisOptions.ts
index e21301f..3af663f 100644
--- a/shared/src/lib/reporter/types/AnalysisOptions.ts
+++ b/shared/src/lib/reporter/types/AnalysisOptions.ts
@@ -1,7 +1,30 @@
 interface AnalysisOptions {
-    includeBrilliant?: boolean;
-    includeCritical?: boolean;
-    includeTheory?: boolean;
+
+    includeBrilliant?:
+        boolean;
+
+    includeCritical?:
+        boolean;
+
+    includeTheory?:
+        boolean;
+
+
+    /*
+     * Rating online del jugador
+     * correspondiente a esta partida.
+     *
+     * Se utilizar├í para adaptar
+     * determinados umbrales contextuales,
+     * especialmente Miss.
+     */
+    whiteRating?:
+        number;
+
+    blackRating?:
+        number;
+
 }
 
+
 export default AnalysisOptions;
\ No newline at end of file
