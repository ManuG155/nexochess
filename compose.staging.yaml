name: nexochess-staging

services:
  app:
    build: .
    ports:
      - "8081:8080"
    env_file:
      - .env
    depends_on:
      - database

  database:
    image: mongo:latest
    volumes:
      - nexochess-staging-database:/data/db
    attach: false

volumes:
  nexochess-staging-database:
