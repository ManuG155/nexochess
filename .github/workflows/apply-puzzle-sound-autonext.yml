name: Apply puzzle sound and auto-next

on:
  pull_request:
    types: [synchronize]

permissions:
  contents: write

jobs:
  apply:
    if: github.event.pull_request.head.ref == 'agent/show-full-game-importer'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0

      - name: Detect completed patch
        id: detect
        shell: bash
        run: |
          if grep -q 'NEXO_PUZZLE_SOUND_AUTONEXT_POLISH' client/src/apps/features/puzzles/pages/Puzzles/Puzzles.module.css; then
            echo 'applied=true' >> "$GITHUB_OUTPUT"
          else
            echo 'applied=false' >> "$GITHUB_OUTPUT"
          fi

      - name: Apply patch
        if: steps.detect.outputs.applied == 'false'
        run: python .github/scripts/apply-puzzle-sound-autonext.py

      - name: Install dependencies
        if: steps.detect.outputs.applied == 'false'
        run: npm ci

      - name: Verify
        if: steps.detect.outputs.applied == 'false'
        run: npm run verify

      - name: Commit
        if: steps.detect.outputs.applied == 'false'
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add client/src/lib/boardSounds.ts client/src/apps/features/puzzles/pages/Puzzles/index.tsx client/src/apps/features/puzzles/pages/Puzzles/Puzzles.module.css client/public/locales/*/puzzles.json
          git commit -m 'feat(puzzles): add board sounds and automatic advance [puzzle-sound-autonext-applied]'
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
