name: CI

on:
  push:
    branches: [master, main, develop, staging]
  pull_request:
    branches: [master, main, develop, staging]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Verify and build
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Audit translations
        run: npm run check:translations

      - name: Audit repository contents
        run: npm run check:repository

      - name: Type-check client
        run: npm run check -w client

      - name: Type-check server
        run: npm run check -w server

      - name: Build production bundles
        run: npm run build

      - name: Validate Cloudflare Worker syntax
        run: |
          node --check cloudflare/worker.mjs
          node --check cloudflare/puzzles.mjs
          node --check scripts/export-puzzles-d1-sql.mjs

      - name: Prepare Cloudflare static assets
        run: node scripts/build-cloudflare.mjs
